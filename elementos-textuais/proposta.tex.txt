\chapter{Proposta de Solução} \label{chap:proposta}

% Este capítulo apresenta a abordagem proposta para o descarregamento de tarefas em redes de Computação de Borda Veicular (\gls{VEC}). A solução consiste em um agente de Aprendizado por Reforço Profundo (\gls{DRL}) baseado no algoritmo \gls{PPO}, cujo espaço de observação é estruturado de forma invariante à escala por meio do método multicritério \gls{FUZZY-TOPSIS}.

Este capítulo apresenta a proposta de solução para o problema de descarregamento de tarefas em ambientes veiculares, considerando a comunicação tanto com dispositivos de borda quanto diretamente entre os dispositivos instalados nos veículos. O desafio central abordado nesta modelagem é a não estacionariedade, um fenômeno comum aos sistemas multiagente resultante da interação e da coadaptação contínua entre os agentes~\cite{albrecht2024}, além disso, também são consideradas os comportamentos complexos e variáveis de incerteza, tais como a mobilidade veicular, cargas de trabalho baseadas em traços reais de uso, limitações de processamento (\textit{throttling}) e atrasos na propagação de informações. 

% Consequentemente, a introdução desses elementos estabelece um ambiente de simulação rigoroso e estocástico, projetado para imitar as limitações operacionais e os ruídos de comunicação enfrentados em cenários reais~\cite{albrecht2024}.

\section{Ambiente}

\begin{figure}[htb]
    \centering
    \resizebox{\textwidth}{!}{%
    \begin{tikzpicture}[
        font=\sffamily,
        >=Stealth,
        % --- Pic: carro visto de cima indo para a direita ---
        car right/.pic={
            \begin{scope}[scale=0.6]
                \filldraw[fill=white, draw=black, rounded corners=3pt, thick]
                    (-1.8,-0.75) rectangle (1.8,0.75);
                \filldraw[fill=black!60, draw=black, thick]
                    (-0.55,-0.55) -- (-1.05,-0.45) -- (-1.05,0.45) -- (-0.55,0.55) -- cycle;
                \filldraw[fill=black!60, draw=black, thick]
                    (0.45,-0.55) -- (0.95,-0.45) -- (0.95,0.45) -- (0.45,0.55) -- cycle;
                \filldraw[fill=black!25, draw=black, thick]
                    (-0.55,-0.55) rectangle (0.45,0.55);
                \filldraw[fill=yellow!80, draw=none]
                    (1.55,-0.65) rectangle (1.75,-0.35);
                \filldraw[fill=yellow!80, draw=none]
                    (1.55,0.35) rectangle (1.75,0.65);
            \end{scope}
        },
        % --- Pic: carro visto de cima indo para cima ---
        car up/.pic={
            \begin{scope}[scale=0.6]
                \filldraw[fill=white, draw=black, rounded corners=3pt, thick]
                    (-0.75,-1.8) rectangle (0.75,1.8);
                \filldraw[fill=black!60, draw=black, thick]
                    (-0.55,-0.45) -- (-0.45,-0.95) -- (0.45,-0.95) -- (0.55,-0.45) -- cycle;
                \filldraw[fill=black!60, draw=black, thick]
                    (-0.55,0.55) -- (-0.45,1.05) -- (0.45,1.05) -- (0.55,0.55) -- cycle;
                \filldraw[fill=black!25, draw=black, thick]
                    (-0.55,-0.45) rectangle (0.55,0.55);
                \filldraw[fill=yellow!80, draw=none]
                    (-0.65,1.55) rectangle (-0.35,1.75);
                \filldraw[fill=yellow!80, draw=none]
                    (0.35,1.55) rectangle (0.65,1.75);
            \end{scope}
        },
        % --- Pic: semaforo ---
        semaforo/.pic={
            \begin{scope}[scale=0.45]
                \filldraw[fill=gray!70, draw=black, thick, rounded corners=3pt]
                    (-0.55,-1.6) rectangle (0.55,1.6);
                \fill[red!90] (0, 1.0) circle (0.38);
                \fill[yellow!90] (0, 0) circle (0.38);
                \fill[green!70!black] (0,-1.0) circle (0.38);
                \draw[gray!60, line width=4pt] (0,-1.6) -- (0,-2.5);
            \end{scope}
        },
        label text/.style={font=\scriptsize\bfseries, align=center, fill=white, draw=black!50, rounded corners=1pt, inner sep=2pt}
    ]
        % ---- Canvas: (0,0) a (20,18) ----
        \clip (0,0) rectangle (20,18);

        % ---- Fundo (gramado) ----
        \fill[green!18] (0,0) rectangle (20,18);

        % ---- Ruas (2 faixas cada) ----
        % Horizontal: y = 6 a 10
        \fill[black!45] (0,6) rectangle (20,10);
        % Vertical: x = 8 a 12
        \fill[black!45] (8,0) rectangle (12,18);
        % Cruzamento
        \fill[black!45] (8,6) rectangle (12,10);

        % ---- Marcações de faixa ----
        \draw[line width=2.2pt, white, dashed, dash pattern=on 12pt off 10pt] (10,0)  -- (10,6);
        \draw[line width=2.2pt, white, dashed, dash pattern=on 12pt off 10pt] (10,10) -- (10,18);
        \draw[line width=2.2pt, white, dashed, dash pattern=on 12pt off 10pt] (0,8)   -- (8,8);
        \draw[line width=2.2pt, white, dashed, dash pattern=on 12pt off 10pt] (12,8)  -- (20,8);

        % ---- V1: Agente (indo para a direita, faixa inferior) ----
        \pic at (3.5,7) {car right};
        \node[fill=blue!15, draw=blue!70, rounded corners=2pt, thick,
              font=\small\bfseries, align=center, inner sep=3pt]
            (lagente) at (3.5,4.5) {Agente (V1)};
        \draw[blue!70, thick] (lagente.north) -- (3.5,6.4);
        % seta mobilidade
        \draw[->, very thick, orange!90!black] (5.3,7.0) -- (7.4,7.0);
        \node[fill=white, draw=orange!80, rounded corners=2pt,
              font=\scriptsize\bfseries, inner sep=2pt]
            at (6.3,5.6) {Mobilidade Din\^amica};
        \draw[orange!70, thin] (6.3,5.88) -- (6.3,6.55);

        % ---- V2 (indo para a esquerda, faixa superior) ----
        \pic[rotate=180] at (16.5,9) {car right};
        \draw[->, very thick, orange!90!black] (14.7,9.0) -- (12.6,9.0);
        \node[fill=white, draw=gray!60, rounded corners=2pt,
              font=\scriptsize\bfseries, inner sep=2pt]
            at (16.5,11.5) {V2};
        \draw[gray!60, thin] (16.5,11.2) -- (16.5,9.7);

        % ---- V3 (indo para cima, faixa direita da via vertical) ----
        \pic at (11,2.5) {car up};
        \draw[->, very thick, orange!90!black] (11,4.3) -- (11,5.6);
        \node[fill=white, draw=gray!60, rounded corners=2pt,
              font=\scriptsize\bfseries, inner sep=2pt]
            at (13.8,2.5) {V3};
        \draw[gray!60, thin] (13.1,2.5) -- (11.7,2.5);

        % ---- Semáforo + Edge Server ----
        \pic at (7.3,11.2) {semaforo};
        \node[draw=black, fill=red!8, thick, rounded corners=4pt,
              align=center, font=\small\bfseries, inner sep=5pt]
            (edge) at (4.0,14.2) {Edge Server\\(Dispositivo de Borda)};
        \draw[thick, gray!70] (7.3,11.8) -- (edge.south east);

        % ---- Carga de Trabalho (canto inferior esquerdo) ----
        \node[draw=blue!70, fill=blue!5, thick, rounded corners=4pt,
              align=center, font=\scriptsize\bfseries, inner sep=5pt]
            (workload) at (2.5,2.0) {Carga de Trabalho\\(Tra\c{c}os Reais / Estoc\'astico)};
        \draw[->, dotted, thick, blue!70] (workload.north east) -- (3.0,6.2);

        % ---- \gls{V2I} / V2E ----
        \draw[<->, dashed, line width=1.5pt, red!70!black]
            (3.5,7.8) -- (edge.south);
        \node[fill=white, draw=red!60, rounded corners=2pt,
              font=\scriptsize\bfseries, text=red!80!black, inner sep=2pt]
            at (2.2,11.5) {V2I / V2E};
        \node[fill=white, font=\scriptsize, text=red!70!black, inner sep=1pt]
            at (2.2,10.8) {Atraso de Propaga\c{c}\~ao};

        % ---- Throttling ----
        \node[draw=red!70, fill=white, dashed, thick, rounded corners=3pt,
              align=center, font=\scriptsize\bfseries]
            (throttle) at (4.5,16.8) {\textit{Throttling}\\Limita\c{c}\~oes de Processamento};
        \draw[->, dotted, thick, red!70] (throttle.south) -- (edge.north);

        % ---- \gls{V2V}: Agente <-> V3 ----
        \draw[<->, dashed, line width=1.5pt, purple!80]
            (4.8,6.4) -- (10.3,3.5);
        \node[fill=white, draw=purple!70, rounded corners=2pt,
              font=\scriptsize\bfseries, text=purple!80, inner sep=2pt]
            at (7.3,4.3) {V2V};

        % ---- \gls{V2V}: Agente <-> V2 ----
        \draw[<->, dashed, line width=1.5pt, purple!80]
            (5.2,7.6) -- (14.6,8.5);
        \node[fill=white, draw=purple!70, rounded corners=2pt,
              font=\scriptsize\bfseries, text=purple!80, inner sep=2pt]
            at (10.0,9.0) {V2V};

        % ---- Não-Estacionariedade (canto inferior direito) ----
        \node[fill=white, draw=purple!70, thick, rounded corners=3pt,
              align=center, font=\scriptsize\bfseries, text=purple!80, inner sep=4pt]
            at (15.5,4.0) {Intera\c{c}\~ao Multiagente\\N\~ao-Estacionariedade};

        % ---- Legenda (canto superior direito) ----
        \node[draw=black!50, fill=gray!5, very thick, dashed, rounded corners=4pt,
              align=left, font=\footnotesize, inner sep=7pt, anchor=north east]
            at (19.5,17.8) {
            \textbf{Restri\c{c}\~oes do Ambiente Simulado:}\\[2pt]
            $\bullet$ Mobilidade Din\^amica Veicular\\
            $\bullet$ Chegada Estoc\'astica de Tarefas\\
            $\bullet$ Atrasos e Ru\'idos na Comunica\c{c}\~ao\\
            $\bullet$ Gargalos de Processamento (\textit{Throttling})\\
            $\bullet$ Fen\^omeno de N\~ao-Estacionariedade
        };

        % ---- Borda ----
        \draw[thick] (0,0) rectangle (20,18);
    \end{tikzpicture}%
    }
    \caption{Representação do ambiente de simulação: um cruzamento veicular contemplando comunicação \gls{V2I}, \gls{V2V} e os principais componentes do cenário estocástico.}
    \label{fig:ambiente_simulacao}
\end{figure}

O ambiente de treinamento e avaliação é implementado como um \gls{DES} desenvolvido em C++20 com co-rotinas, utilizando a biblioteca \texttt{simcpp20}~\cite{simcpp20}. Cada tarefa gerada pelos veículos é modelada como um processo concorrente que: (i)~aguarda o instante de chegada $t_j$; (ii)~consome o estado global do sistema para construir o vetor de observação; (iii)~executa a política de escalonamento; (iv)~atualiza as filas dos servidores; e (v)~registra as métricas reais de \gls{JCT} e consumo energético.

O servidor \gls{VEC} é modelado utilizando um processador com \gls{DVFS} e \textit{clock rate} de $F_{\text{VEC}} = 1{,}2\,\text{GHz}$, com fila compartilhada de capacidade $Q_{\max} = 100$ tarefas. Os veículos possuem frequências heterogêneas, amostradas no intervalo $[0{,}3;\,1{,}5]\,\text{GHz}$. A comunicação \gls{V2I} utiliza largura de banda $B = 20\,\text{MHz}$, potência de transmissão $P_{\text{tx}} = 0{,}2\,\text{W}$ e modelo de perda de percurso conforme \gls{3GPP} TR 38.901 (UMi Street Canyon). As mesmas especificações valem para \gls{V2V}, com altura de antena de $1{,}5\,\text{m}$ e fator de ruído de $9\,\text{dB}$.

\section{Cenários de Teste e Treinamento}

Os cenários de treinamento foram gerados utilizando o SUMO~\cite{lopez2018microscopic} para a mobilidade veicular e para carga de trabalho utilizou-se um \textit{trace} real de demandas computacionais do Alibaba~\cite{cheng2018characterizing}, realizando as conversões de escala de tempo para adequação ao cenário veicular. Já para o cenário de testes, foi utilizada a mesma mobilidade e foram criados 658 cenários a partir do \textit{trace} original com carga de trabalho distribuídos conforme a Tabela~\ref{tab:cenarios}.

\begin{table}[htb]
\centering
\caption{Distribuição dos 658 cenários de treinamento/teste por taxa de chegada.}
\label{tab:cenarios}
\begin{tabular}{lrr}
\toprule
\textbf{Taxa (tps)} & \textbf{Qtd.\ cenários} & \textbf{Tarefas/cenário} \\
\midrule
10  &  59 & 100 \\
15  &  90 & 150 \\
20  & 120 & 200 \\
25  & 150 & 250 \\
30  & 180 & 300 \\
45  &  59 & 450 \\
\midrule
\textbf{Total} & \textbf{658} & --- \\
\bottomrule
\end{tabular}
\end{table}

Cada cenário define, para cada tarefa, suas características, o instante de chegada e o veículo de origem. O ambiente compartilha um conjunto de parâmetros físicos fixos entre todos os cenários, calibrados para refletir as restrições operacionais de redes veiculares reais, como: frequências de processamento heterogêneas, alcances de comunicação limitados, potência de transmissão controlada e capacidade finita de filas. A Tabela~\ref{tab:system-params} apresenta esses parâmetros.

\begin{table}[htb]
\centering
\caption{Parâmetros físicos do ambiente simulado.}
\label{tab:system-params}
\begin{tabular}{llr}
\toprule
\textbf{Parâmetro} & \textbf{Descrição} & \textbf{Valor} \\
\midrule
$F_{\text{VEC}}$      & Frequência de clock da \gls{VEC}                & $1{,}2\,\text{GHz}$   \\
$F_{\text{veh}}$      & Freq.\ dos veículos (heterogênea)         & $[0{,}3;\,1{,}5]\,\text{GHz}$ \\
$B_{\text{V2I/V2V}}$  & Largura de banda dos canais               & $20\,\text{MHz}$      \\
$P_{\text{tx}}$       & Potência de transmissão \gls{V2I} e \gls{V2V}         & $0{,}2\,\text{W}$    \\
$R_{\max}$            & Raio de cobertura \gls{VEC}                     & $300\,\text{m}$      \\
$V_{\text{V2V}}$      & Alcance \gls{V2V}                               & $300\,\text{m}$      \\
% $\alpha_c$            & Coef.\ energia dinâmica (CMOS)            & $6 \times 10^{-9}$   \\
% $\eta_{\text{DL}}$    & Fator de multiplicação do \textit{deadline} & $1{,}2\times D_j$ \\
$Q_{\max}$            & Capacidade máxima das filas               & $100$ tarefas        \\
\bottomrule
\end{tabular}
\end{table}

\section*{Protocolo de Treinamento e Hiperparâmetros}

O treinamento de cada política \gls{DRL} é conduzido interagindo com o simulador, sem nenhum acesso aos cenários de teste. Um único cenário de 100 tarefas a 10~tps (tarefas por segundo) é utilizado durante fase. A generalização para outros volumes e taxas é avaliada nas 658 instâncias de teste (\textit{zero-shot}). Cada episódio corresponde a uma simulação completa do cenário (100 transições). O treinamento dura 400 episódios (40\,000 transições no total). A Tabela~\ref{tab:sac-hparams} lista os hiperparâmetros do \gls{SAC} utilizados em todos os experimentos.

% A cada nova transição armazenada no \textit{replay buffer} (capacidade $= 50\,000$), o agente executa uma atualização dos pesos via mini-lote de $1\,024$ amostras, após um aquecimento de $1\,024$ transições. 

\begin{table}[htb]
\centering
\caption{Hiperparâmetros do \gls{SAC} compartilhados por todas as políticas \gls{DRL}.}
\label{tab:sac-hparams}
\begin{tabular}{llr}
\toprule
\textbf{Hiperparâmetro} & \textbf{Descrição} & \textbf{Valor} \\
\midrule
$\eta$ (lr)             & Taxa de aprendizado (Adam)         & $3 \times 10^{-4}$ \\
$\gamma$                & Fator de desconto                  & $0{,}99$           \\
$\tau$                  & Taxa de atualização \textit{soft}  & $0{,}005$          \\
$\alpha_{\text{ent}}$   & Temperatura de entropia            & $0{,}10$           \\
$|\mathcal{B}|$         & Capacidade do \textit{replay buffer} & $50\,000$        \\
$B$                     & Tamanho do mini-lote               & $1\,024$           \\
\textit{warmup}         & Transições antes do 1º update      & $1\,024$           \\
Neurônios por camada    & Arquitetura \gls{MLP} (2 camadas ocultas)& $64$               \\
% \midrule
% Custo normalizado & Média de $\text{JCT}/JCT^{\text{max}}$ e $E/E^{\text{max}}$ & $\tfrac{1}{2}(\cdot + \cdot)$ \\
% $\gamma_p$ (penalidade) & Penalidade por violação de \textit{deadline} & $0{,}30$ \\
\bottomrule
\end{tabular}
\end{table}

% ============================================================
\section{Visão Geral da Arquitetura e Formulação do Problema}
% ============================================================
O problema de descarregamento em redes \gls{VEC} é formulado como um \gls{POMDP}. O objetivo central é maximizar a taxa de tarefas concluídas dentro do prazo rigoroso (\textit{deadline}), ponderada com o custo normalizado de latência e energia. A ponderação assimétrica ($w_s > w_c$) reflete o requisito de \gls{QoS} crítico em redes veiculares: violar prazos de aplicações de segurança é mais grave do que incorrer em custo energético adicional. Formalmente, a função objetivo é formulada da seguinte maneira:

\begin{equation}
    \max_{\pi}\; J(\pi) = w_s \cdot \underbrace{\mathbb{E}_\pi\!\left[\mathbf{1}(\text{JCT}_i \leq D_i)\right]}_{\text{taxa de sucesso}} - w_c \cdot \underbrace{\mathbb{E}_\pi\!\left[\frac{1}{2}\!\left(\frac{\text{JCT}_i}{\text{JCT}^{\max}_i} + \frac{E_i}{E^{\text{ref}}_i}\right)\right]}_{\text{custo normalizado}\;\in\,[0,1]}
    \label{eq:obj_geral}
\end{equation}
onde $w_s = 0{,}60 > w_c = 0{,}40$ refletem a maior importância dada à taxa de sucesso. O custo normalizado é a média aritmética de dois termos, ambos obtidos por normalização \textit{min-max} com mínimo zero e máximo por tarefa: $\text{JCT}_i/\text{JCT}^{\max}_i \in [0,1]$ e $E_i/E^{\text{ref}}_i \in [0,1]$, onde
\begin{align*}
    \text{JCT}^{\max}_i &= \max\!\bigl(\widehat{\text{JCT}}^{\ell}_i,\; \widehat{\text{JCT}}^{\text{rsu}}_i \cdot \mathbf{1}_{\text{viável}},\; \widehat{\text{JCT}}^{\text{v2v}}_i \cdot \mathbf{1}_{\text{viável}}\bigr) \\
    E^{\text{ref}}_i &= \max\!\bigl(E^{\ell}_i,\; E^{\text{rsu}}_i \cdot \mathbf{1}_{\text{viável}},\; E^{\text{v2v}}_i \cdot \mathbf{1}_{\text{viável}}\bigr)
\end{align*}
são o pior caso de latência e de energia entre os modos viáveis para a tarefa $i$.

Para resolver esse problema, a arquitetura proposta combina \gls{TOPSIS} e \gls{SAC} em um \textit{pipeline} de decisão unificado. A cada nova tarefa, o módulo \gls{TOPSIS} avalia os nós de computação disponíveis segundo os critérios de latência, energia e distância, e identifica o melhor candidato (\textit{rank}$_1$). O resultado é condensado em um vetor de estado compacto de 6~dimensões $\mathbf{S}_t \in \mathbb{R}^6$, invariante ao número de candidatos presentes. Uma \gls{MLP} treinada via \gls{SAC} recebe esse vetor e emite a decisão binária,  $a_t \in \{0, 1\}$, executar localmente ou descarregar para o \textit{rank}$_1$. 

% O treinamento ocorre em um simulador estocástico que reproduz a heterogeneidade dos nós de computação, a dinâmica das filas e as perdas de percurso segundo os padrões \gls{3GPP} TR~38.901 (\gls{V2I}) e TR~37.885 (\gls{V2V}). 

\begin{algorithm}[h!]
\LinesNumbered
\caption{Decisão de Descarregamento \gls{SAC}-\gls{TOPSIS} (inferência)}
\label{alg:sac-topsis}
\SetKwInOut{Input}{Entrada}
\SetKwInOut{Output}{Saída}
\Input{Tarefa $i$: complexidade $C_i$, \textit{deadline} $D_i$, posição $\mathbf{x}_i$;\\
       Conjunto de $N$ candidatos $\mathcal{M} = \{m_1,\ldots,m_N\}$ (\gls{V2I} e \gls{V2V});\\
       \textit{Actor} \gls{MLP} treinado $\pi_\theta$; filas $Q_{\ell}$, $Q_{\mathrm{r1}}$; $Q_{\max}$, $C_{\max}$.}
\Output{Ação $a_t \in \{0,\,1\}$: executar localmente ou descarregar para \textit{rank}$_1$.}
\BlankLine
\tcp{Passo 1 --- Construção da matriz de decisão}
\For{$k = 1$ \KwTo $N$}{
    $x_{k1} \gets \widehat{\mathrm{JCT}}(m_k)$\tcp*{latência estimada (ms)}
    $x_{k2} \gets E(m_k)$\tcp*{energia total estimada (J)}
    $x_{k3} \gets \|\mathbf{x}_i - \mathbf{x}_{m_k}\|$\tcp*{distância física (m)}
}
\BlankLine
\tcp{Passo 2 --- Ranqueamento via Algoritmo~\ref{alg:topsis} (todos critérios de custo)}
$\mathbf{w} \gets [0{,}40;\; 0{,}30;\; 0{,}30]$\;
$\mathbf{C}^* \gets \text{Algoritmo~\ref{alg:topsis}}(\mathbf{X},\, \mathbf{w})$\tcp*{todos os critérios são de custo}
$\mathrm{rank}_1 \gets \arg\max_k\; C^*_k$\;
\BlankLine
\tcp{Passo 3 --- Construção do vetor de estado compacto 6D}
$\Delta_{\mathrm{obj}} \gets \bigl(\widehat{\mathrm{JCT}}_{\ell} - \widehat{\mathrm{JCT}}_{\mathrm{r1}}\bigr) \;/\; D_i$\;
$\mathbf{S}_t \gets \left[\,\Delta_{\mathrm{obj}},\;
    \tfrac{\widehat{\mathrm{JCT}}_{\ell}}{D_i};
    \tfrac{\widehat{\mathrm{JCT}}_{\mathrm{r1}}}{D_i};
    \tfrac{Q_{\mathrm{r1}}}{Q_{\max}},\;
    \tfrac{Q_{\ell}}{Q_{\max}},\;
    \tfrac{C_i}{C_{\max}}\,\right]$\;
\BlankLine
\tcp{Passo 4 --- Inferência e decisão}
$a_t \gets \arg\max_{a}\; \pi_\theta(a \mid \mathbf{S}_t)$\;
\eIf{$a_t = 0$}{
    \Return \textsc{local} \tcp*{executa no próprio veículo}
}{
    \Return \textsc{offload} para $\mathrm{rank}_1$ \tcp*{\gls{VEC} ou \gls{V2V}}
}
\end{algorithm}

O Algoritmo~\ref{alg:sac-topsis} descreve o fluxo completo de decisão da arquitetura \gls{SAC}-\gls{TOPSIS} em quatro passos. No passo~1, constrói-se a matriz $\mathbf{X} \in \mathbb{R}^{N \times 3}$ com latência estimada, energia e distância de cada candidato. No passo~2, invoca-se o Algoritmo~\ref{alg:topsis} com pesos $\mathbf{w}$ e todos os critérios de custo, obtendo o vetor de escores $\mathbf{C}^*$; o candidato de maior escore torna-se o \textit{rank}$_1$. No passo~3, o resultado é comprimido no vetor de estado $\mathbf{S}_t \in \mathbb{R}^6$, cujos componentes capturam a vantagem relativa do descarregamento ($\Delta_{\mathrm{obj}}$), as urgências local e do \textit{rank}$_1$ em relação ao \textit{deadline}, as ocupações normalizadas das filas e a complexidade da tarefa --- sendo $C_i$ e $D_i$ inseridos diretamente, sem passar pelo \gls{TOPSIS}. Por fim, no passo~4, o \textit{Actor} \gls{MLP} ($6 \to 64 \to 64 \to 2$, ReLU, Softmax) recebe $\mathbf{S}_t$ e emite a decisão binária $a_t$: $a_t = 0$ corresponde à execução local; $a_t = 1$, ao descarregamento para o \textit{rank}$_1$.


% ============================================================
\section*{Módulo \texorpdfstring{\gls{TOPSIS}}{\gls{TOPSIS}} e a Engenharia de Estado Compacto}
% ============================================================
Um dos desafios no desenvolvimento de soluções em cenários \gls{VEC} é a variabilidade do número de candidatos ao descarregamento ao longo do tempo. Abordagens baseadas em Grafos (\gls{GNN}) resolvem isso ao custo de alta complexidade computacional~\cite{miriyala2026}. Neste trabalho, para lidar com esse problema de forma menos custosa, propõe-se extrair as características invariantes à escala através do \gls{TOPSIS}, reduzindo os candidatos a um único vetor de estado de dimensão fixa. Isto é feito, avaliando os nós candidatos por meio de três critérios, conforme a Tabela~\ref{tab:topsis-criterios}.

\begin{table}[htb]
\centering
\caption{Critérios e pesos do módulo \gls{TOPSIS}.}
\label{tab:topsis-criterios}
\begin{tabular}{clcc}
\hline
\textbf{Critério} & \textbf{Descrição} & \textbf{Tipo} & \textbf{Peso} \\
\hline
$C_1$ & Tempo de conclusão estimado ($\widehat{\text{JCT}}$ em milisegundos) & Custo & $w_1 = 0{,}40$ \\
$C_2$ & Energia total (processamento + transmissão em Joules) & Custo & $w_2 = 0{,}30$ \\
$C_3$ & Distância física ao executor (metros) & Custo & $w_3 = 0{,}30$ \\
\hline
\end{tabular}
\end{table}

O procedimento segue os passos clássicos do \gls{TOPSIS}: (i)~normalização vetorial da matriz de decisão por norma $L_2$ de cada coluna; (ii)~ponderação por $w_j$; (iii)~identificação das soluções ideal positiva $A^+$ (mínimo de cada critério) e ideal negativa $A^-$ (máximo), ambas calculadas por minimização; (iv)~cálculo das distâncias de cada alternativa a $A^+$ e $A^-$; (v)~escore de proximidade. Independentemente de existirem $2$ ou $50$ candidatos, o estado entregue à rede neural é sempre um vetor fixo de 6 dimensões construído a partir do resultado do \gls{TOPSIS}:
\begin{equation}
    \mathbf{S}_t = \left[\Delta_{\text{obj}},\; \frac{\widehat{\text{JCT}}_{\text{local}}}{D_i},\; \frac{\widehat{\text{JCT}}_{\text{rank}_1}}{D_i},\; \frac{|Q_{\text{rank}_1}|}{Q_{\max}},\; \frac{|Q_{\text{local}}|}{Q_{\max}},\; \frac{C_i}{C_{\max}}\right] \in \mathbb{R}^6
    \label{eq:estado6d}
\end{equation}

Cada dimensão carrega uma semântica irredutível para a decisão. $\Delta_{\text{obj}}$ denota a diferença normalizada entre o custo estimado de execução local e o custo do candidato \textit{rank}$_1$ segundo o \gls{TOPSIS}; valor positivo indica que o \textit{offload} é vantajoso. $\widehat{\text{JCT}}_{\text{local}}/D_i$ calcula a razão entre o tempo de conclusão estimado localmente e o prazo, de modo que valores $> 1{,}0$ sinalizam violação iminente. $\widehat{\text{JCT}}_{\text{rank}_1}/D_i$ é a razão análoga para o melhor candidato, permitindo ao agente comparar diretamente as duas opções. $|Q_{\text{rank}_1}|/Q_{\max}$ representa a ocupação normalizada da fila do nó classificado em primeiro lugar pelo \gls{TOPSIS}, indicando deterioração futura da latência de \textit{offload} caso o agente decida descarregar. $|Q_{\text{local}}|/Q_{\max}$ informa a ocupação da fila local, refletindo o custo de aguardar execução no próprio dispositivo. Por fim, $C_i/C_{\max}$ normaliza o número de ciclos de \gls{CPU} da tarefa, ancorando o contexto computacional da decisão. Em conjunto, as seis dimensões fornecem ao agente os sinais necessários para avaliar, a cada instante, a vantagem relativa do \textit{offload} frente à execução local.

% \section*{Seleção da Ação de Descarregamento}
O agente \gls{SAC} opera sobre um espaço de ação discreto binário $a_t \in \{0, 1\}$. O mapeamento entre ação e modo de execução é mediado pelo resultado do \gls{TOPSIS}. Quando $a_t = 1$, a tarefa é encaminhada ao candidato melhor ranqueado, que pode ser o servidor \gls{VEC} ou um veículo vizinho. Caso nenhum candidato de \textit{offload} seja viável ($N = 0$), $a_t = 1$ é tratado como \textsc{LOCAL} por padrão. Essa separação de responsabilidades — \gls{TOPSIS} decide \textit{para onde} descarregar, \gls{SAC} decide \textit{se} descarregar — reduz o espaço de busca do agente de $N+1$ para $2$ ações, independente de $N$.

% \begin{equation}
%     \text{modo}_t =
%     \begin{cases}
%         \text{LOCAL} & \text{se } a_t = 0 \\
%         \text{rank}_1 \in \{\text{VEC},\,\text{V2V}\} & \text{se } a_t = 1
%     \end{cases}
%     \label{eq:acao}
% \end{equation}


Como o \gls{TOPSIS} comprime qualquer conjunto de $N$ candidatos em um único vetor $\mathbb{R}^6$ constante, a política treinada interpola naturalmente para cenários com topologias distintas sem retreinamento. A Figura~\ref{fig:mlp-arch} detalha como o vetor $\mathbf{S}_t$ alimenta a rede \textit{Actor} do \gls{SAC} e como a camada de saída produz a decisão binária.

\begin{figure}[htb]
\centering
\resizebox{\textwidth}{!}{%
\begin{tikzpicture}[
    font=\sffamily\small,
    >=Stealth,
    nin/.style={circle, draw=orange!70!black, thick, fill=orange!30,
                minimum size=0.65cm, inner sep=0pt},
    nhid/.style={circle, draw=blue!50!black, thick, fill=blue!20,
                 minimum size=0.65cm, inner sep=0pt},
    nout/.style={circle, draw, thick, minimum size=0.72cm, inner sep=0pt},
    lbl/.style={font=\sffamily\footnotesize, align=left},
    ew/.style={->, gray!55, line width=0.4pt},
]

%% Camada de entrada: 6 neurônios
\foreach \i/\name/\desc in {
    1/{$s_1$}/{$\Delta_{\mathrm{obj}}$},
    2/{$s_2$}/{$\widehat{\mathrm{JCT}}_{\ell}/D_i$},
    3/{$s_3$}/{$\widehat{\mathrm{JCT}}_{\mathrm{r1}}/D_i$},
    4/{$s_4$}/{$Q_{\mathrm{r1}}/Q_{\max}$},
    5/{$s_5$}/{$Q_{\ell}/Q_{\max}$},
    6/{$s_6$}/{$C_i/C_{\max}$}
}{
    \node[nin] (I\i) at (0, {-(\i-1)*1.0}) {\name};
    \node[lbl, right=0.06cm of I\i] {\desc};
}

%% Camada oculta 1
\foreach \i in {1,2,3,4,5}{
    \node[nhid] (H1\i) at (4.2, {-(\i-1)*1.0}) {};
}
\node[font=\sffamily\small, gray] at (4.2, {-5.0+0.15}) {$\vdots$};
\node[font=\sffamily\scriptsize, gray, below=0.04cm of H15] {($64$ neur.)};

%% Camada oculta 2
\foreach \i in {1,2,3,4,5}{
    \node[nhid] (H2\i) at (7.2, {-(\i-1)*1.0}) {};
}
\node[font=\sffamily\small, gray] at (7.2, {-5.0+0.15}) {$\vdots$};
\node[font=\sffamily\scriptsize, gray, below=0.04cm of H25] {($64$ neur.)};

%% Camada de saída
\node[nout, fill=green!25, draw=green!60!black] (O1) at (10.0, -0.8) {$a_0$};
\node[nout, fill=red!20,   draw=red!60!black]   (O2) at (10.0, -3.2) {$a_1$};

%% Rótulos de saída
\node[lbl, right=0.2cm of O1] {\textbf{LOCAL} --- executa no veículo};
\node[lbl, right=0.2cm of O2] {\textbf{OFFLOAD} --- rank$_1$: \gls{VEC} ou V2V};

%% Conexões entrada -> H1
\foreach \i in {1,...,6}{
    \foreach \j in {1,...,5}{ \draw[ew] (I\i.east) -- (H1\j.west); }
}
%% Conexões H1 -> H2
\foreach \i in {1,...,5}{
    \foreach \j in {1,...,5}{ \draw[ew] (H1\i.east) -- (H2\j.west); }
}
%% Conexões H2 -> saída
\foreach \i in {1,...,5}{
    \draw[ew] (H2\i.east) -- (O1.west);
    \draw[ew] (H2\i.east) -- (O2.west);
}

%% Rótulos das camadas
\node[font=\sffamily\bfseries\footnotesize, above=0.45cm of I1]  {Entrada ($\mathbb{R}^6$)};
\node[font=\sffamily\bfseries\footnotesize, above=0.45cm of H11] {Oculta 1 (ReLU)};
\node[font=\sffamily\bfseries\footnotesize, above=0.45cm of H21] {Oculta 2 (ReLU)};
\node[font=\sffamily\bfseries\footnotesize] at (10.0, 0.45)      {Saída (Softmax)};

%% Chave lateral
\draw[decorate, decoration={brace, amplitude=5pt, mirror}, thick, gray!65]
    (I6.south west) -- (I1.north west)
    node[midway, left=7pt, font=\sffamily\scriptsize, align=right]
    {$\mathbf{S}_t \in \mathbb{R}^6$\\(saída do \gls{TOPSIS})};

\end{tikzpicture}%
}
\caption{Arquitetura da rede \textit{Actor} (\gls{MLP} $6 \to 64 \to 64 \to 2$): o vetor de estado compacto $\mathbf{S}_t$ produzido pelo \gls{TOPSIS} alimenta duas camadas ocultas com ativação ReLU; a camada Softmax final emite as probabilidades das ações $a_0$ (LOCAL) e $a_1$ (\textit{OFFLOAD} \textit{rank}$_1$).}
\label{fig:mlp-arch}
\end{figure}

% ============================================================
% \section*{Modelagem da Recompensa e Seleção do Algoritmo}
% ============================================================
% \subsection{Engenharia de Recompensa (\textit{Reward Shaping})}
\section*{Modelagem da Recompensa}

A função de recompensa é formulada para alinhar o sinal de aprendizado diretamente com o objetivo da Equação~\ref{eq:obj_geral}, isto é, priorizar a taxa de sucesso e, secundariamente, minimizar o custo operacional. Para isso, a recompensa por tarefa é estruturada de duas maneiras conforme:
\begin{equation}
    r_i =
    \begin{cases}
        1{,}0 - \dfrac{1}{4}\!\left(\dfrac{\widehat{\text{JCT}}_i}{D_i} + \dfrac{E_i}{E^{\text{ref}}_i}\right), & \text{se } \widehat{\text{JCT}}_i \leq D_i \quad (\in [0{,}5,\;1{,}0]) \\[8pt]
        -1{,}0 - \min\!\left(1{,}0,\; \dfrac{\widehat{\text{JCT}}_i - D_i}{D_i}\right), & \text{se } \widehat{\text{JCT}}_i > D_i \quad (\in [-2{,}0,\;-1{,}0])
    \end{cases}
    \label{eq:reward}
\end{equation}
onde $E^{\text{ref}}_i = \max\!\bigl(E^{\ell}_i,\; E^{\text{rsu}}_i \cdot \mathbf{1}_{\text{viável}},\; E^{\text{v2v}}_i \cdot \mathbf{1}_{\text{viável}}\bigr)$ é a energia máxima entre os modos viáveis para a tarefa $i$, calculada por tarefa e por instante --- não é uma constante global, pois depende de quais modos estão viáveis no momento da decisão. 

% Quando $\widehat{\text{JCT}}_t \leq D_i$, condição que garante que a razão $\widehat{\text{JCT}}_t/D_i$ permaneça no intervalo $[1]$. Como o termo $E_t/E^{\text{max}}_i$ também pertence a $[1]$ por construção, a média de ambos assegura que a recompensa de sucesso $r_t$ varie estritamente entre $[0{,}5,\,1{,}0]$. Por outro lado, quando $\widehat{\text{JCT}}_t > D_i$, aplica-se exclusivamente o regime de falha.

A formulação da recompensa introduz intencionalmente uma lacuna mínima de 1,5 unidades entre o pior cenário de sucesso e o melhor cenário de falha. Essa descontinuidade atua como uma estratégia de \textit{reward shaping} para estabelecer que cumprir o prazo é sempre preferível a qualquer falha. Sem essa separação, o agente poderia aprender a tolerar violações leves de prazo em troca de maior eficiência energética~\cite{ng1999policy}. Além disso, essa transição abrupta na fronteira do prazo justifica a necessidade de adotar algoritmos de aprendizado estabilizados por entropia, como o \gls{SAC}~\cite{haarnoja2018sac}, visto que abordagens puramente baseadas em valor enfrentariam problemas severos de superestimação nessa região \cite{miriyala2026}.

% A hierarquia de sinal da Equação~\ref{eq:reward} implementa diretamente os pesos $w_s > w_c$ da Equação~\ref{eq:obj_geral}: a lacuna mínima entre qualquer recompensa de falha e qualquer recompensa de sucesso é $1{,}5$ unidades, o que torna o respeito ao \textit{deadline} a barreira dominante na superfície de decisão. Dentro do regime de sucesso, a recompensa é continuamente modulada pela média normalizada $\tfrac{1}{2}(\text{JCT}/D_i + E/E^{\text{max}}_i)$, incentivando a eficiência mesmo quando o prazo é cumprido. O fator de desconto temporal foi fixado em $\gamma = 0{,}99$ para que o agente antecipe o efeito cascata do enfileiramento de tarefas.

% \subsection{Justificativa do Algoritmo \texorpdfstring{\gls{SAC}}{\gls{SAC}}}
% A função de recompensa bimodal da Equação~\ref{eq:reward} induz uma superfície de decisão com descontinuidade severa na fronteira $\widehat{\text{JCT}} = D_i$. Algoritmos baseados em valor, como o \gls{DQN}, são propensos à superestimação catastrófica de $Q$-values próximos a essa fronteira~\cite{miriyala2026}. Algoritmos \textit{on-policy} como o \gls{PPO} apresentam baixa eficiência amostral em ambientes com recompensa esparsa por região de falha.

% O \gls{SAC} (Soft \textit{Actor}-\textit{Critic}) resolve ambos os problemas simultaneamente. Sua formulação entrópica maximiza $J(\pi) + \alpha_{\text{temp}}\,\mathcal{H}(\pi)$, o que garante exploração robusta nas vizinhanças da fronteira de \textit{deadline} sem convergência prematura. O uso de \textit{twin Q-networks} (redes $Q_1$ e $Q_2$) com atualização via mínimo reduz viés de superestimação. Por ser \textit{off-policy}, o \gls{SAC} reutiliza experiências passadas via \textit{replay buffer}, conferindo eficiência amostral superior ao \gls{PPO} em ambientes com recompensa rara, como o regime de sucesso deste problema.

% ============================================================
\section{Complexidade Computacional}
% ============================================================
Ao evitar o uso de \glspl{GNN} e optar pelo precondicionamento via \gls{TOPSIS}, a complexidade assintótica da tomada de decisão a cada instante é regida pelas operações de avaliação e ordenação. A avaliação dos $N$ candidatos sob os $M$ critérios possui complexidade $O(N \times M)$, enquanto a ordenação final das alternativas opera em $O(N \log N)$. Como o número de critérios $M$ é constante, a complexidade dominante do pré-processamento escalar cresce de forma log-linear com a densidade veicular. Em seguida, a inferência da \gls{MLP} (\textit{Actor network}), com duas camadas ocultas de $64$ neurônios e entrada compacta de 6 dimensões, opera com complexidade $O(1)$ em relação ao número de candidatos. Essa arquitetura de rede requer um custo paramétrico extremamente baixo. Dessa forma, a abordagem torna-se altamente escalável e compatível com os requisitos de latência e com as restrições de \textit{hardware} menos robustos.

% ============================================================
\section{Estudo de Ablação}
\label{sec:ablacao}
% ============================================================

% Um estudo de ablação é uma técnica experimental de análise causal que consiste em remover ou substituir sistematicamente um componente do sistema proposto — mantendo todos os demais fixos — e medir o impacto no desempenho. Cada variante responde à pergunta: \emph{"quanto esse componente contribui para o resultado?"} Se o desempenho cai significativamente com sua remoção, o componente é essencial; se cai pouco, pode ser redundante. A técnica é amplamente empregada em aprendizado de máquina para validar que cada escolha de design — arquitetura, função de recompensa, representação de estado — justifica sua presença no sistema final.

Para isolar a contribuição de cada componente do \gls{SAC}-\gls{TOPSIS}, realizou-se um estudo de ablação controlado com quatro variantes treinadas e avaliadas nas mesmas 658 instâncias de cenário. Cada variante remove ou substitui um único componente de design em relação à proposta completa, mantendo os demais fixos.

% % ─────────────────────────────────────────────────────────────
% \subsection{Descrição Detalhada das Variantes}
% % ─────────────────────────────────────────────────────────────

% Nesta subseção são apresentadas todas as variantes da proposta que foram utilizadas no estudo de ablação. Ao final, serão apresentadas as tabelas que resumem as características das variantes.

\subsubsection*{SAC-Base — Referência}

O \gls{SAC}-Base é o ponto de partida do estudo e serve de referência para medir o ganho de cada componente. Utiliza um estado bruto de 11 dimensões que concatena diretamente as métricas do ambiente sem nenhum pré-processamento multicritério:
\begin{equation}
    \mathbf{S}^{\text{base}} = \left[
        C_i,\;
        D_i,\;
        d_{\text{VEC}},\;
        f_{\text{dvfs}},\;
        Q_{\text{VEC}},\;
        T_{\text{cpu}},\;
        f_{\text{local}},\;
        Q_{\text{local}},\;
        f_{\text{V2V}},\;
        Q_{\text{V2V}},\;
        d_{\text{V2V}}
    \right]
    \label{eq:estado-base}
\end{equation}
onde $d$ denota distância, $f$ frequência de processamento, $Q$ ocupação de fila e $T_{\text{cpu}}$ temperatura do processador. Embora possam existir múltiplos vizinhos \gls{V2V} no alcance, o estado expõe apenas um, o veículo com maior frequência de \textit{clock} dentro do raio \gls{V2V}, selecionado por uma varredura gulosa no pré-processamento do cenário. Quando nenhum vizinho está disponível, as três últimas componentes recebem o valor sentinela $-1$ para distinguir ausência de \gls{V2V} de \gls{V2V} com métrica zero.

O espaço de ação é ternário $a_t \in \{0\!=\!\textsc{local},\, 1\!=\!\textsc{vec},\, 2\!=\!\textsc{v2v}\}$, ou seja, o agente deve aprender simultaneamente a decisão de descarregar e a escolha do destino. A recompensa é calculada pelo Método de Soma Ponderada (WSM) com pesos iguais sobre três critérios de custo — \gls{JCT} estimado ($j=1$), energia total ($j=2$) e ocupação de fila do executor escolhido ($j=3$):

\begin{equation}
    r^{\text{wsm}}_{t+1} = 2\,\max\!\left(0,\, \min\!\left(1,\, 1 - \bar{x}_a\right)\right) - 1, \quad
    \bar{x}_a = \frac{1}{3}\sum_{j=1}^{3}\tilde{x}_{aj}, \quad
    \tilde{x}_{aj} = \frac{x_{aj}}{\max_{a'} x_{a'j}}
    \label{eq:reward-wsm}
\end{equation}

onde $\tilde{x}_{aj} = x_{aj}/\max_{a'} x_{a'j} \in [0,1]$ normaliza o critério $j$ da alternativa $a$ pelo valor máximo desse critério entre todas as alternativas $a' \in \{\textsc{local},\,\textsc{vec},\,\textsc{v2v}\}$; $\bar{x}_a \in [0,1]$ é a média dos três custos normalizados da alternativa escolhida; e a recompensa é obtida pela transformação $2(1-\bar{x}_a)-1$, que inverte o custo e o remapeia linearmente para $[-1,1]$. Esse sinal de recompensa não distingue violações de \textit{deadline} de execuções lentas porém viáveis.Uma ação que ultrapassa o prazo com custo moderado pode receber recompensa positiva, desorientando o aprendizado em ambientes com prazos rígidos.

%  --- custo mínimo ($\bar{x}_a=0$) produz $r=+1$ e custo máximo ($\bar{x}_a=1$) produz $r=-1$. 
 
%  A expressão $\max(0,\min(1,\cdot))$ é uma salvaguarda numérica: como $1-\bar{x}_a \in [0,1]$ por construção, o clamp é redundante mas evita instabilidades por arredondamento.


\subsubsection*{SAC-A1 — Redução do Espaço de Ação sem TOPSIS}

A variante A1 mantém inalterados o estado 11D (Equação~\ref{eq:estado-base}) e a recompensa WSM (Equação~\ref{eq:reward-wsm}), alterando \emph{apenas} o espaço de ação para binário $a_t \in \{0\!=\!\textsc{local},\, 1\!=\!\textsc{offload}\}$. Quando $a_t = 1$, o destino de \textit{offload} é determinado por uma regra de prioridade estática — \gls{VEC} primeiro (se viável), \gls{V2V} caso contrário — sem qualquer análise multicritério:
\begin{equation}
    \text{modo}^{\text{A1}}_t =
    \begin{cases}
        \textsc{local} & \text{se } a_t = 0 \\
        \textsc{vec}   & \text{se } a_t = 1 \text{ e \gls{VEC} viável} \\
        \textsc{v2v}   & \text{se } a_t = 1,\; \text{\gls{VEC} inviável e \gls{V2V} viável} \\
        \textsc{local} & \text{se } a_t = 1 \text{ e nenhum offload viável (fallback)}
    \end{cases}
    \label{eq:acao-a1}
\end{equation}
O objetivo é isolar o efeito de reduzir o espaço de busca de 3 para 2 ações, sem nenhum critério inteligente de escolha de destino. A regra \gls{VEC}$\succ$\gls{V2V} é estática e ignora as condições dinâmicas de fila e distância no momento da decisão.

\subsubsection*{SAC-S1 — Estado Compacto 6D Manual sem TOPSIS}

A variante S1 mantém o espaço de ação ternário e a recompensa \gls{WSM} do \textit{baseline}, mas substitui o estado 11D por um vetor compacto de 6 dimensões considerando as características de maior relevância:

\begin{equation}
    \mathbf{S}^{\text{S1}} = \left[
        \frac{C_i}{C_{\max}},\;
        \min\!\left(1,\, \frac{\widehat{\text{JCT}}_{\text{local}}}{D_i}\right),\;
        \frac{d_{\text{VEC}}}{d_{max}},\;
        \min\!\left(1,\, \frac{|Q_{\text{VEC}}|}{Q_{max}}\right),\;
        \min\!\left(1,\, \frac{|Q_{\text{local}}|}{Q_{max}}\right),\;
        \frac{f_{\text{local}}}{f_{\text{VEC}}}
    \right]
    \label{eq:estado-s1}
\end{equation}

O primeiro componente calcular o valor relativo dos ciclos da tarefa. O segunda componente, urgência relativa $\widehat{\text{JCT}}_{\text{local}}/D_i$, verifica se a execução local da tarefa vai estourar o prazo. Nessa formulação, não é incluída informações de \gls{V2V} (frequência, fila, distância do vizinho), forçando o agente a decidir entre o servidor \gls{VEC} e \gls{V2V} apenas obsevando o comportamento do ambiente veicular. 

\subsubsection*{SAC-R1 — Recompensa Deadline-Aware sem Mudança de Estado ou Ação}

A variante R1 mantém o estado 11D (Equação~\ref{eq:estado-base}) e o espaço de ação ternário idênticos ao \textit{baseline}, alterando apenas a função de recompensa para a formulação sensível ao prazo conforme a Equação \ref{eq:reward}.

% \begin{equation}
%     r^{\text{R1}}_i =
%     \begin{cases}
%         1{,}0 - \dfrac{1}{4}\!\left(\dfrac{\widehat{\text{JCT}}_i}{D_i} + \dfrac{E_i}{E^{\text{max}}_i}\right), & \widehat{\text{JCT}}_i \leq D_i \quad (\in [0{,}5,\;1{,}0]) \\[6pt]
%         -1{,}0 - \min\!\left(1{,}0,\; \dfrac{\widehat{\text{JCT}}_i - D_i}{D_i}\right), & \widehat{\text{JCT}}_i > D_i \quad (\in [-2{,}0,\;-1{,}0])
%     \end{cases}
%     \label{eq:reward-r1}
% \end{equation}
% A lacuna mínima de $1{,}5$ unidades entre o maior valor de falha ($-1{,}0$) e o menor valor de sucesso ($+0{,}5$) cria uma barreira de \textit{deadline} dominante na função de valor do agente. Quando o prazo é cumprido, a recompensa é contínua e modulada pela média normalizada $\tfrac{1}{2}(\widehat{\text{JCT}}_i/D_i + E_i/E^{\text{ref}}_i)$, incentivando eficiência mesmo dentro do regime de sucesso. Quando violado, a penalidade cresce proporcionalmente ao excesso de tempo, evitando o platô de recompensa negativa constante do WSM e ensinando o agente a "falhar por pouco" se inevitável. O objetivo é medir o ganho atribuível \emph{exclusivamente} à função de recompensa, sem alterar o estado nem o espaço de ação.

\subsubsection*{\gls{SAC}-T1 — \texorpdfstring{\gls{TOPSIS}}{\gls{TOPSIS}} como \textit{Feature} de Estado com Ação de 3 Classes}

A variante T1 combina o estado compacto 6D calculado \emph{via} \gls{TOPSIS} (idêntico ao da proposta, Equação~\ref{eq:estado6d}) com a recompensa \textit{deadline}-aware (Equação~\ref{eq:reward}), mas mantém o espaço de ação ternário $a_t \in \{0\!=\!\textsc{local},\, 1\!=\!\textsc{vec},\, 2\!=\!\textsc{v2v}\}$. O \gls{TOPSIS} calcula internamente os escores de todos os candidatos e usa o resultado para construir o vetor de estado — incluindo a vantagem objetiva $\Delta_{\text{obj}}$ e o \gls{JCT} normalizado do \textit{rank}$_1$ — mas a decisão final de escolher \gls{VEC} ou \gls{V2V} permanece com o agente \gls{SAC}. Essa variante permite quantificar separadamente quanto do ganho do \gls{SAC}-\gls{TOPSIS} provém do \gls{TOPSIS} como compressor de estado semântico versus o \gls{TOPSIS} como responsável pela decisão de destino do descarregamento.

% ─────────────────────────────────────────────────────────────
\section*{Decomposição Causal}
% ─────────────────────────────────────────────────────────────


\begin{table}[htb]
\centering
\caption{Resultados das variantes do estudo de ablação avaliadas em 658 cenários de teste. A proposta completa é destacada em negrito.}
\label{tab:ablacao-resultados}
\resizebox{\linewidth}{!}{%
\begin{tabular}{lllllrrr}
\toprule
\textbf{Variante} & \textbf{Estado} & \textbf{Ação} & \textbf{Recompensa} & \textbf{Taxa de Sucesso(\%)} & \textbf{SWQ} & \textbf{JCT (s)} \\
\midrule
SAC-Base  & 11D bruto   & Ternária & \gls{WSM}                & 4,52  & 0,0119 & 57,075 \\
SAC-A1    & 11D bruto   & Binária  & \gls{WSM}                & 39,11 & 0,1256 & 13,722 \\
SAC-S1    & 6D   & Ternária & \gls{WSM}                & 28,69 & 0,1122 & 39,298 \\
SAC-R1    & 11D bruto   & Ternária & \textit{Deadline-aware} & 38,26 & 0,1223 & 13,353 \\
SAC-T1    & 6D \gls{TOPSIS} & Ternária & \textit{Deadline-aware} & 32,96 & 0,1270 & 11,487 \\
\midrule
\textbf{SAC-TOPSIS} & \textbf{6D \gls{TOPSIS}} & \textbf{Binária} & \textbf{\textit{Deadline-aware}} & \textbf{45,21} & \textbf{0,1484} & \textbf{6,547} \\
\bottomrule
\end{tabular}}
\end{table}

A decomposição causal consiste em medir o impacto isolado de cada mudança no desempenho. A Equação~\ref{eq:decomposicao} sintetiza o caminho sequencial percorrido do \gls{SAC}-Base ao \gls{SAC}-\gls{TOPSIS}, onde cada seta quantifica a variação absoluta em pontos percentuais (pp) da taxa de sucesso devida àquela transição.

\begin{equation}
    \underbrace{4{,}52\%}_{\text{SAC-Base}}
    \xrightarrow{+24{,}17\text{pp}}
    \underbrace{28{,}69\%}_{\text{SAC-S1}}
    \xrightarrow{+4{,}27\text{pp}}
    \underbrace{32{,}96\%}_{\text{SAC-T1}}
    \xrightarrow{+12{,}25\text{pp}}
    \underbrace{45{,}21\%}_{\text{SAC-TOPSIS}}
    \label{eq:decomposicao}
\end{equation}

O caminho revela que as três modificações não contribuem de forma uniforme. A maior parcela do ganho provém da compressão de estado (primeiro salto, $+24{,}17$~pp), seguida pelo uso do \gls{TOPSIS} como responsável pela decisão de destino (terceiro salto, $+12{,}25$~pp). A incorporação do \gls{TOPSIS} como \textit{feature} de estado combinada com a recompensa \textit{deadline-aware} (segundo salto) contribui de forma mais modesta ($+4{,}27$~pp), pois o agente ainda precisava resolver por conta própria a ambiguidade \gls{VEC}/\gls{V2V}. 

% A Tabela~\ref{tab:ablacao-decomposicao} detalha a interpretação causal de cada transição, relacionando o ganho numérico ao mecanismo subjacente que o explica.

% \begin{table}[htb]
% \centering
% \caption{Decomposição causal dos ganhos ao longo do caminho de ablação.}
% \label{tab:ablacao-decomposicao}
% \resizebox{\linewidth}{!}{%
% \begin{tabular}{p{3.8cm} c p{8.5cm}}
% \toprule
% \textbf{Componente (transição)} & \textbf{$\Delta$ taxa} & \textbf{Interpretação} \\
% \midrule
% Estado 6D compacto manual (Base $\to$ S1) &
%     $+24{,}17$ pp &
%     Substituir o vetor 11D bruto por 6 \textit{features} semanticamente compactas reduz a dimensionalidade e melhora a generalização entre cenários \\[4pt]
% \gls{TOPSIS} como \textit{feature} + \textit{deadline} reward (S1 $\to$ T1) &
%     $+4{,}27$ pp &
%     Incorporar o escore $\Delta_{\text{obj}}$ e o \gls{JCT} do \textit{rank}$_1$ no estado e substituir WSM pela recompensa \textit{deadline-aware} acrescenta ganho limitado pois o agente ainda precisa resolver a escolha \gls{VEC} vs.\ \gls{V2V} \\[4pt]
% \gls{TOPSIS} como responsável pela decisão (T1 $\to$ \gls{SAC}-\gls{TOPSIS}) &
%     $+12{,}25$ pp &
%     Transferir a escolha de destino do agente para o \gls{TOPSIS} e reduzir o espaço de ação de 3 para 2 é o maior salto marginal: elimina a ambiguidade \gls{VEC}/\gls{V2V} e garante que o destino seja sempre o candidato de menor custo multicritério \\
% \midrule
% \textbf{Ganho total} & $\mathbf{+40{,}69}$ \textbf{pp} & \gls{SAC}-\gls{TOPSIS} vs.\ \gls{SAC}-Base \\
% \bottomrule
% \end{tabular}}
% \end{table}

% Para isolar o efeito da recompensa \textit{deadline-aware} sem alterar o estado, a variante \gls{SAC}-R1 atinge $38{,}26\%$ ($+33{,}74$ pp sobre o \textit{baseline} com estado 11D e ação ternária). Esse valor é maior que o de S1 ($28{,}69\%$), evidenciando que o sinal de recompensa assimétrico é individualmente mais poderoso que a compressão de estado quando aplicados em isolamento. Entretanto, no caminho sequencial, a maior parte do ganho de R1 é compartilhada com S1: o estado 6D já codifica urgência temporal em $\widehat{\text{JCT}}/D_i$, de modo que acrescentar a recompensa \textit{deadline}-aware sobre S1 produz um incremento marginal de apenas $+4{,}27$ pp (transição S1$\to$T1, que combina as duas mudanças).

\section{Análise dos Resultados}

Três achados principais emergem do estudo. Primeiro, a variante \gls{SAC}-A1 ($39{,}2\%$) demonstra que binarizar a ação \emph{sem} critério de seleção de destino já supera o \textit{baseline} em $+34{,}6$ pp — a simples redução do espaço de busca tem efeito expressivo — mas não alcança a proposta ($45{,}3\%$) porque a regra estática \gls{VEC}$\succ$\gls{V2V} ignora condições dinâmicas de fila e distância no momento da decisão. Segundo, a variante \gls{SAC}-T1 ($33{,}1\%$) mostra que usar o \gls{TOPSIS} apenas como \textit{feature} de estado contribui com $+4{,}3$ pp sobre S1, enquanto usar o \gls{TOPSIS} como \textit{prior} de decisão (\gls{SAC}-\gls{TOPSIS}) adiciona $+12{,}2$ pp sobre T1: a eficácia do \gls{TOPSIS} é quase três vezes maior quando ele assume o controle do destino do que quando fornece apenas informação semântica ao agente. Terceiro, o \gls{JCT} médio do \gls{SAC}-\gls{TOPSIS} ($6{,}6$ s) é $8{,}6\times$ menor que o do \gls{SAC}-Base ($57{,}1$ s), indicando que a combinação \gls{TOPSIS}$+$estado compacto$+$reward \textit{deadline}-aware produz não apenas mais tarefas concluídas no prazo, mas também latências substancialmente menores.

Em suma, a hierarquia de contribuição dos componentes é:
\[
    \Delta_{\text{estado}} > \Delta_{\text{TOPSIS-decisão}} > \Delta_{\text{reward+feature}} \gg \Delta_{\text{ação-binária-estática}}
\]
confirmando que a \emph{compressão de estado via TOPSIS} e o \emph{TOPSIS como prior de destino} são os dois pilares do ganho do \gls{SAC}-\gls{TOPSIS}, e que a recompensa \textit{deadline}-aware, embora poderosa isoladamente, tem contribuição marginal reduzida quando combinada com um estado já semanticamente rico.

\subsection{Métrica de Efetividade: \textit{Success-Weighted Quality} (SWQ)}

A taxa de \textit{deadline} ($\hat{\tau}$) empregada até aqui é uma métrica binária: conta quantas tarefas foram concluídas no prazo, mas ignora \emph{quão bem} essas tarefas foram atendidas. Uma política que cumpre o prazo com folga de $1\%$ é tratada da mesma forma que uma que o cumpre com folga de $40\%$. De maneira análoga, o \gls{JCT} médio e a energia total acumulam contribuições de tarefas bem-sucedidas e de tarefas que violaram o prazo, tornando difícil separar eficiência de falha.

Para reunir ambas as dimensões em um único número interpretável, define-se o \textbf{\textit{Success-Weighted Quality}} (SWQ):

\begin{equation}
    \mathrm{SWQ}(\pi) = \hat{\tau}(\pi) \;\times\; \overline{\left(\frac{D_j - \mathrm{JCT}_j}{D_j}\right)}_{\!\mathcal{S}}
    \label{eq:swq}
\end{equation}

\noindent onde $\hat{\tau}(\pi) \in [0,1]$ é a taxa de sucesso da política $\pi$ e o segundo fator é a folga normalizada média calculada exclusivamente sobre o conjunto $\mathcal{S}$ de tarefas que cumpriram o prazo:

\[
    \overline{\text{folga}}_{\mathcal{S}} = \frac{1}{|\mathcal{S}|} \sum_{j \in \mathcal{S}} \frac{D_j - \mathrm{JCT}_j}{D_j} \;\in\; (0,\,1]
\]

A folga normalizada vale $1$ quando a tarefa é concluída instantaneamente e decresce para $0$ quando o \gls{JCT} se aproxima do prazo. Uma política que jamais erra o prazo e conclui todas as tarefas com folga máxima teria $\mathrm{SWQ} = 1{,}0$; uma que sempre viola teria $\mathrm{SWQ} = 0$. O produto das duas componentes penaliza simultaneamente políticas com baixa taxa de sucesso \emph{e} políticas que cumprem o prazo por margem ínfima — características indesejadas em contextos veiculares com prazos rígidos.

A Figura~\ref{fig:swq} apresenta o SWQ médio das vinte políticas avaliadas sobre os 661 cenários de teste. O \gls{SAC}-\gls{TOPSIS} alcança o maior valor ($\mathrm{SWQ} = 0{,}1484$), seguido pelas políticas \textit{Always-Local}, \gls{PPO}-Base e \gls{PPO}-Compact (todas em $0{,}1418$). Nota-se que o \textit{greedy-unfair} — que possui conhecimento perfeito das filas em tempo de execução — obtém $\mathrm{SWQ} = 0{,}1261$, inferior ao \gls{SAC}-\gls{TOPSIS}: embora o oráculo minimize o \gls{JCT} por decisão, sua natureza míope acumula pior qualidade agregada do que a política aprendida. Por outro lado, \textit{Always-VEC} e \gls{SAC}-Base apresentam os menores SWQs ($0{,}003$ e $0{,}012$, respectivamente), reflexo de taxas de sucesso irrisórias ($1{,}9\%$ e $4{,}6\%$).

\begin{figure}[htb]
\centering
\includegraphics[width=\textwidth]{figuras/performance_09_swq.png}
\caption{SWQ médio por política sobre 661 cenários de teste ($\alpha=0{,}35$, $\beta=0{,}35$, $\gamma=0{,}30$). Barras em cinza representam heurísticas; barras escuras representam políticas \gls{DRL}. A borda dourada indica a melhor política. Maior é melhor.}
\label{fig:swq}
\end{figure}

\section{Por que o \texorpdfstring{\gls{TOPSIS}}{\gls{TOPSIS}} agrega valor ao \texorpdfstring{\gls{SAC}}{\gls{SAC}} mas não ao \texorpdfstring{\gls{PPO}}{\gls{PPO}}?}
\label{sec:sac-topsis-sinergia}

Os resultados experimentais revelam uma assimetria marcante: o \gls{TOPSIS} eleva a taxa de \textit{deadline} do \gls{SAC} de $4{,}6\%$ (\gls{SAC}-Base) para $45{,}3\%$ (\gls{SAC}-\gls{TOPSIS}), um ganho de $+40{,}7$ pontos percentuais, enquanto o \gls{PPO} com \gls{TOPSIS} regride. Para isolar a causa desta assimetria, foram realizados dois experimentos controlados. Primeiro, o \gls{PPO}-\gls{TOPSIS} original com estado 12D e ação ternária --- que permitia ao agente escolher entre \textit{rank}$_1$, \textit{rank}$_2$ e \textit{rank}$_3$ --- obteve $18{,}4\%$, abaixo do \gls{PPO}-Base ($42{,}4\%$). Segundo, o \gls{PPO}-\gls{TOPSIS} foi refatorado para utilizar \emph{exatamente} o mesmo estado 6D, ação binária $\{0\!=\!\text{LOCAL},\, 1\!=\!\text{rank}_1\}$ e função de recompensa do \gls{SAC}-\gls{TOPSIS}, diferindo apenas no algoritmo de otimização (\gls{PPO} vs.\ \gls{SAC}). O resultado deste experimento controlado foi igualmente $18{,}4\%$, com a política colapsando para $86\%$ de \textit{offload} via \textit{rank}$_1$ --- o oposto do \gls{PPO}-Compact ($100\%$ LOCAL), porém igualmente disfuncional. O \gls{PPO}-\gls{TOPSIS}-Compact, que usa o estado 6D mas foi treinado em cenários distintos, apenas iguala o \gls{PPO}-Base. Em síntese: \textbf{o \gls{TOPSIS} é uma alavanca decisiva para o \gls{SAC} e um fator neutro ou prejudicial para o \gls{PPO}, independentemente do design do estado}. Esta seção analisa as causas estruturais desta assimetria.

\subsection{O problema do aprendizado redundante no PPO-TOPSIS}

O \gls{PPO}-\gls{TOPSIS} foi implementado com um estado de 12 dimensões --- três alternativas (LOCAL, \gls{VEC}, \gls{V2V}) ordenadas pelo escore \gls{TOPSIS}, cada uma descrita por quatro atributos normalizados (\gls{JCT} estimado, energia, distância, \textit{link lifetime}) --- e um espaço de ação ternário $a_t \in \{0\!=\!\text{rank}_1,\, 1\!=\!\text{rank}_2,\, 2\!=\!\text{rank}_3\}$. Esse design cria um problema de aprendizado redundante: o \gls{TOPSIS} já determina deterministicamente que o candidato na posição 0 é o melhor segundo critérios multicritério rigorosos. O agente \gls{PPO}, portanto, é treinado por reforço para redescobrir, por tentativa e erro, o que o \gls{TOPSIS} já computou de forma ótima. A informação de valor genuíno que o agente deveria aprender --- se há benefício em descarregar para o melhor candidato dado o estado da fila e a urgência do \textit{deadline} --- está enterrada sob o sinal dominante de que \textit{rank}$_1$ é sempre superior a \textit{rank}$_2$ e \textit{rank}$_3$ em custo multicritério.

O \gls{SAC}-\gls{TOPSIS} resolve este problema por projeto: delega a pergunta \emph{``para onde descarregar?''} inteiramente ao \gls{TOPSIS} e reserva ao agente apenas a pergunta \emph{``descarregar ou não?''} --- uma decisão genuinamente nova, que o \gls{TOPSIS} não responde, pois envolve a comparação entre o melhor candidato de \textit{offload} e a execução local ponderada pela urgência temporal, pelo congestionamento da fila e pela vantagem objetiva $\Delta_{\text{obj}}$ codificada no estado 6D.

\subsection{Colapso de política induzido pelo sinal TOPSIS}

O segundo mecanismo de falha do \gls{PPO}-\gls{TOPSIS} é o colapso de política. Com o estado 12D, a posição \textit{rank}$_1$ exibe consistentemente \gls{JCT} menor, energia menor e distância menor que \textit{rank}$_2$ e \textit{rank}$_3$. Isso gera um gradiente de política fortemente unidirecional desde as primeiras iterações: $P(a\!=\!0) \to 1$. O \gls{PPO} utiliza coeficiente de entropia $\alpha_{\text{ent}} = 0{,}01$, dez vezes menor que o do \gls{SAC} ($\alpha_{\text{ent}} = 0{,}10$ com ajuste automático via gradiente dual). Com entropia tão fraca, uma vez que a distribuição de política colapsa para $P(\text{rank}_1) \approx 1$, as trajetórias coletadas nas iterações seguintes são quase exclusivamente de escolhas \textit{rank}$_1$, reforçando o colapso. O mecanismo de \textit{clipping} do \gls{PPO} ($\epsilon = 0{,}2$) não reverte esse estado: ele apenas impede mudanças \emph{grandes} por passo, mas não impede a deriva acumulada durante os 400 episódios de treinamento.

O \gls{SAC}, ao contrário, opera sobre um espaço de ação binário em que nenhuma das duas opções (LOCAL ou \textit{offload} para \textit{rank}$_1$) domina o outro de forma incondicional: há cenários em que a fila do \gls{VEC} está saturada, ou o \gls{V2V} está distante, ou o \textit{deadline} é tão apertado que mesmo o melhor candidato de \textit{offload} não chega a tempo. Nesses casos, a resposta correta é LOCAL, e o sinal de recompensa \textit{deadline}-aware --- com lacuna mínima de $1{,}5$ entre o melhor valor de falha e o pior valor de sucesso --- preserva um gradiente exploratório relevante. A alta temperatura entrópica do \gls{SAC} garante que ambas as ações recebam probabilidade não nula ao longo do treinamento, evitando o colapso.

\subsection{Assimetria de eficiência amostral com cenário único}

O protocolo de treinamento emprega um único cenário (\texttt{cenario\_v2x\_10tps\_w01}) durante os 400 episódios, com generalização \textit{zero-shot} para os 661 cenários de teste. Essa escolha é compatível com o \gls{SAC}, mas estruturalmente adversa ao \gls{PPO}.

O \gls{SAC} mantém um \textit{replay buffer} de 50.000 transições e realiza atualizações com \textit{minibatch} de 1024 amostras por passo. Com aproximadamente 100 tarefas por episódio, as 40.000 transições coletadas ao longo dos 400 episódios são amostradas em média $\approx\!40$ vezes cada durante o treinamento --- uma reciclagem intensiva que extrai máxima informação de um conjunto de dados limitado. Além disso, a mistura de experiências de diferentes pontos do treinamento (política mais aleatória nos primeiros episódios, mais refinada nos últimos) cria uma diversidade implícita que previne o sobreajuste ao único cenário de treino.

O \gls{PPO}, sendo \textit{on-policy}, descarta cada lote de trajetórias após as $k\!=\!8$ épocas de atualização. Com um único cenário, cada episódio gera essencialmente o mesmo padrão de chegada de tarefas, o mesmo volume de tarefas e as mesmas condições de canal. A política converge rapidamente para a estratégia que maximiza a recompensa nesse cenário específico --- que, com a salência do sinal \gls{TOPSIS}, é \emph{``sempre escolha rank$_1$''} --- e não tem como escapar desse ótimo local por falta de diversidade nos dados on-policy.

A evidência experimental confirma esta hipótese: o \gls{PPO}-\gls{TOPSIS}-Compact, que usa o estado 6D do \gls{SAC}-\gls{TOPSIS} (idêntico em conteúdo semântico) mas com algoritmo \gls{PPO}, atinge $42{,}4\%$ --- o mesmo que o \gls{PPO}-Base, que usa o estado bruto 11D. O estado \gls{TOPSIS} mais informativo não agrega valor ao \gls{PPO} porque o gargalo não é a qualidade do estado, mas a ineficiência amostral do algoritmo on-policy com cenário único.

\subsection{Síntese: complementaridade estrutural SAC--TOPSIS}

A superioridade do \gls{SAC}-\gls{TOPSIS} não decorre de nenhum componente isolado, mas da complementaridade estrutural entre os dois módulos em três dimensões:

\begin{enumerate}
    \item \textbf{Divisão de responsabilidades}: O \gls{TOPSIS} resolve deterministicamente a pergunta \emph{``qual candidato de offload é melhor?''}; o \gls{SAC} aprende a resposta à pergunta complementar \emph{``vale a pena descarregar para esse candidato?''}. As duas perguntas são independentes e igualmente necessárias; nenhum dos dois módulos, isoladamente, as responde ambas.

    \item \textbf{Off-policy + replay buffer}: O \gls{SAC} recicla cada transição $\approx\!40$ vezes no treinamento com um único cenário, alcançando uma eficiência amostral inacessível ao \gls{PPO} on-policy. Essa reciclagem é segura porque o \gls{SAC} aprende uma função de valor $Q(s,a)$ que é off-policy por construção --- ao contrário do \gls{PPO}, cujo gradiente de política requer dados coletados pela política corrente.

    \item \textbf{Regularização entrópica intensa}: O \gls{SAC} mantém $\alpha_{\text{ent}} = 0{,}10$ com ajuste automático, dez vezes superior ao coeficiente fixo do \gls{PPO}. Essa entropia elevada é precisamente o que impede o colapso de política frente ao sinal claro fornecido por $\Delta_{\text{obj}}$ no estado 6D --- mantendo a exploração ativa mesmo quando o \gls{TOPSIS} sugere fortemente que \textit{offload} é melhor.
\end{enumerate}

Em contraste, o \gls{PPO}-\gls{TOPSIS} sofre das três falhas simétricas: aprende uma pergunta que o \gls{TOPSIS} já responde (redundância), colapsa para uma estratégia degenerada com entropia insuficiente para resistir ao sinal \gls{TOPSIS} (colapso), e sobreajusta ao único cenário de treino sem o mecanismo de reciclagem do \textit{replay buffer} (ineficiência amostral). O experimento controlado confirma inequivocamente este diagnóstico: ao trocar \emph{apenas} o algoritmo --- mantendo estado 6D, ação binária e recompensa idênticos --- o \gls{SAC}-\gls{TOPSIS} atinge $45{,}3\%$ enquanto o \gls{PPO}-\gls{TOPSIS} obtém $18{,}4\%$, um déficit de $26{,}9$ pontos percentuais atribuível exclusivamente à natureza on-policy do \gls{PPO} e à sua baixa temperatura entrópica. O \gls{TOPSIS}, portanto, não é neutro quando mal integrado: \emph{amplifica} as fraquezas do algoritmo hospedeiro.

% ──────────────────────────────────────────────────────────────────────────────
\section{Avaliação Comparativa de Políticas}
\label{sec:ranking-completo}

As Tabelas~\ref{tab:ranking-completo} e~\ref{tab:ganho-topsis} resumem os resultados de 25 políticas avaliadas em 658 cenários de teste comuns, abrangendo algoritmos de aprendizado por reforço (com e sem \gls{TOPSIS}) e heurísticas de referência. As métricas reportadas são: taxa de \textit{deadline} (\%), SWQ ($= \hat{\tau}\cdot\overline{(D_j - \text{JCT}_j)/D_j}_{\text{sucesso}}$), \gls{JCT} médio em segundos, energia média por tarefa em joules e o objetivo $J(\pi) = 0{,}60\cdot\hat{\tau} - 0{,}40\cdot\overline{\tfrac{1}{2}(\text{JCT}/\text{JCT}^{\max} + E/E^{\text{ref}})}$ (Equação~\ref{eq:obj_geral}), onde $\text{JCT}^{\max}$ e $E^{\text{ref}}$ são os piores casos entre os modos viáveis por tarefa.

\begin{table}[htbp]
\centering
\caption{\textit{Ranking} completo das 25 políticas avaliadas em 658 cenários comuns (ordenado por SWQ decrescente). $\star$~=~usa \gls{TOPSIS}.}
\label{tab:ranking-completo}
\makebox[\linewidth][c]{%
\resizebox{\linewidth}{!}{%
\begin{tabular}{clrrrrrrrrr}
\toprule
\textbf{\#} & \textbf{Política} & \textbf{Cn} & \textbf{Taxa\%} & \textbf{SWQ} & \textbf{\gls{JCT} (s)} & \textbf{E (J)} & \textbf{J($\pi$)} & \textbf{\%Loc} & \textbf{\%V2V} & \textbf{Categoria} \\
\midrule
 1 & sac-topsis $\star$       & 658 & 45,21 & 0,1484 &  6,547 & 25,828 & 0,2454 & 74,1 & 25,9 & \gls{RL}+\gls{TOPSIS} \\
 2 & ppo-topsis $\star$                & 658 & 42,66 & 0,1426 &  5,895 & 21,333 & 0,2344 & 98,7 &  1,3 & \gls{RL}+\gls{TOPSIS} \\
 3 & ppo-base                    & 658 & 42,32 & 0,1418 &  6,045 & 24,414 & 0,2295 & 100,0 &  0,0 & \gls{RL}-Base \\
 4 & ppo-topsis-compact $\star$        & 658 & 42,32 & 0,1418 &  6,045 & 24,414 & 0,2295 & 100,0 &  0,0 & \gls{RL}+\gls{TOPSIS} \\
 5 & td3-base                    & 658 & 42,32 & 0,1418 &  6,045 & 24,414 & 0,2295 & 100,0 &  0,0 & \gls{RL}-Base \\
 6 & a2c-topsis $\star$                & 658 & 42,32 & 0,1418 &  6,045 & 24,414 & 0,2295 & 100,0 &  0,0 & \gls{RL}+\gls{TOPSIS} \\
 7 & td3-topsis $\star$                & 658 & 43,91 & 0,1388 &  3,703 & 22,995 & 0,2424 & 76,5 & 23,5 & \gls{RL}+\gls{TOPSIS} \\
 8 & sac-topsis-9d $\star$             & 658 & 42,82 & 0,1386 &  5,886 & 20,985 & 0,2348 & 94,7 &  5,3 & \gls{RL}+\gls{TOPSIS} \\
 9 & sac-topsis-manhattan $\star$      & 658 & 41,66 & 0,1385 & 11,831 & 23,656 & 0,2211 & 51,1 & 48,9 & \gls{RL}+\gls{TOPSIS} \\
10 & sac-t1 $\star$                    & 658 & 32,96 & 0,1270 & 11,487 & 26,126 & 0,1658 & 22,6 & 77,4 & \gls{RL}+\gls{TOPSIS} \\
11 & greedy-unfair               & 658 & 41,29 & 0,1262 &  5,613 &  7,396 & 0,2370 & 67,3 & 32,7 & Heurística \\
12 & sac-a1                      & 658 & 39,11 & 0,1256 & 13,722 & 21,118 & 0,2064 & 82,9 & 17,1 & \gls{RL}-Base \\
13 & sac-r1                      & 658 & 38,26 & 0,1223 & 13,353 & 31,425 & 0,1914 & 58,8 & 41,2 & \gls{RL}-Base \\
14 & sac-s1                      & 658 & 28,69 & 0,1122 & 39,298 & 20,305 & 0,1210 & 38,2 & 61,8 & \gls{RL}-Base \\
15 & gini                        & 658 & 35,24 & 0,1057 &  3,456 & 39,395 & 0,1790 & 61,7 & 38,3 & Heurística \\
16 & dqn-topsis $\star$                & 658 & 25,90 & 0,0997 & 21,722 & 23,979 & 0,1161 & 20,0 & 80,0 & \gls{RL}+\gls{TOPSIS} \\
17 & jain                        & 658 & 32,54 & 0,0973 &  3,628 & 39,280 & 0,1627 & 58,6 & 41,4 & Heurística \\
18 & random                      & 658 & 30,02 & 0,0950 & 17,853 & 27,330 & 0,1427 & 40,7 & 59,3 & Heurística \\
19 & roundrobin                  & 658 & 28,50 & 0,0905 & 17,900 & 31,080 & 0,1315 & 34,9 & 65,1 & Heurística \\
20 & greedy                      & 658 & 29,03 & 0,0823 & 29,265 &  6,168 & 0,1429 & 42,5 & 57,5 & Heurística \\
21 & a2c-base                    & 658 & 21,74 & 0,0772 & 13,653 & 48,862 & 0,0785 & 20,3 & 79,7 & \gls{RL}-Base \\
22 & dqn-base                    & 658 & 22,10 & 0,0686 & 13,599 & 44,445 & 0,0839 & 21,5 & 78,5 & \gls{RL}-Base \\
23 & greedy-fair                 & 658 & 20,61 & 0,0528 & 40,754 &  5,634 & 0,0823 & 27,9 & 72,1 & Heurística \\
24 & sac-base                    & 658 &  4,52 & 0,0119 & 57,075 & 24,230 & $-$0,042 &  5,1 & 94,9 & \gls{RL}-Base \\
25 & vec                         & 658 &  1,86 & 0,0030 & 144,191 & 4,843 & $-$0,123 &  1,2 & 98,8 & Heurística \\
\bottomrule
\end{tabular}%
}}
\end{table}

A Tabela~\ref{tab:ganho-topsis} isola o efeito do \gls{TOPSIS} para cada família de algoritmo, comparando a versão base com a versão aumentada pelo ranqueamento multicritério.

\begin{table}[htbp]
\centering
\caption{Ganho do \gls{TOPSIS} por família de algoritmo (médias sobre 658 cenários comuns). $\Delta$Taxa e $\Delta$SWQ representam diferença absoluta; $\Delta$J($\pi$) é a diferença na função objetivo.}
\label{tab:ganho-topsis}
\small
\makebox[\linewidth][c]{%
\resizebox{\linewidth}{!}{%
\begin{tabular}{llrrrrrrr}
\toprule
\multirow{2}{*}{Algoritmo} & \multirow{2}{*}{Versão} & \multicolumn{2}{c}{Taxa\%} & \multicolumn{2}{c}{SWQ} & \multicolumn{2}{c}{J($\pi$)} & \multirow{2}{*}{Destaques} \\
\cmidrule(lr){3-4}\cmidrule(lr){5-6}\cmidrule(lr){7-8}
 & & Base & \gls{TOPSIS} & Base & \gls{TOPSIS} & Base & \gls{TOPSIS} & \\
\midrule
\multirow{2}{*}{\gls{SAC}}
 & base   & 4,52  & ---    & 0,0119 & ---    & $-$0,042 & --- & colapso total (94,9\% \gls{V2V}) \\
 & topsis & ---   & 45,21 & ---    & 0,1484 & --- & 0,2454 & $+$40,7 pp / $+$1147\% SWQ \\
\midrule
\multirow{2}{*}{\gls{PPO}}
 & base   & 42,32 & ---    & 0,1418 & ---    & 0,2295 & --- & 100\% local \\
 & topsis & ---   & 42,66  & ---    & 0,1426 & --- & 0,2344 & $+$0,3 pp / $+$0,6\% SWQ \\
\midrule
\multirow{2}{*}{\gls{TD3}}
 & base   & 42,32 & ---    & 0,1418 & ---    & 0,2295 & --- & 100\% local \\
 & topsis & ---   & 43,91  & ---    & 0,1388 & --- & 0,2424 & $+$1,6 pp taxa; \gls{JCT} $-44{,}5\%$ \\
\midrule
\multirow{2}{*}{\gls{DQN}}
 & base   & 22,10 & ---    & 0,0686 & ---    & 0,0839 & --- & 78,5\% \gls{V2V} sem critério \\
 & topsis & ---   & 25,90  & ---    & 0,0997 & --- & 0,1161 & $+$3,8 pp / $+$45\% SWQ \\
\midrule
\multirow{2}{*}{\gls{A2C}}
 & base   & 21,74 & ---    & 0,0772 & ---    & 0,0785 & --- & 79,7\% \gls{V2V}, energia alta \\
 & topsis & ---   & 42,32  & ---    & 0,1418 & --- & 0,2295 & $+$20,6 pp / $+$84\% SWQ \\
\bottomrule
\end{tabular}%
}}
\end{table}


//////////////////////////////////////////////
\chapter{Proposta}
\label{chap:proposta}

Este capítulo apresenta em detalhes a abordagem proposta para o descarregamento de tarefas em redes de \gls{VEC}: um agente de \gls{DRL} baseado no algoritmo \gls{PPO} cujo espaço de observação é construído pelo método \gls{TOPSIS}. A exposição está organizada de forma autossuficiente — todos os conceitos matemáticos necessários são introduzidos e derivados a partir do zero, de modo que o leitor familiarizado com os fundamentos de \gls{RL} e probabilidade básica possa acompanhar o raciocínio sem recorrer a fontes externas.

% ============================================================
\section{Visão Geral da Arquitetura}
\label{sec:visao-geral}
% ============================================================

A solução proposta é estruturada em duas fases distintas: uma \textbf{fase de treinamento} (offline) e uma \textbf{fase de implantação} (online).

Na \textbf{fase de treinamento}, o algoritmo \gls{PPO} interage com um simulador do ambiente \gls{VEC} para otimizar os pesos $\theta$ de uma \textbf{rede neural} (MLP com duas camadas de 256 neurônios) — a política $\pi_\theta$. O \gls{PPO} é, portanto, o \emph{algoritmo de treinamento}; a MLP resultante é o \emph{componente de decisão}. Ao final do treinamento, apenas a MLP é necessária para operação.

Na \textbf{fase de implantação}, o sistema opera em tempo real: para cada tarefa gerada pelo veículo, o \gls{TOPSIS} avalia todos os $N$ candidatos disponíveis — que podem ser \glspl{RSU}, servidores \gls{MEC} ou veículos vizinhos dentro do alcance de comunicação — e constrói a matriz de decisão $\mathbf{X} \in \mathbb{R}^{N \times 4}$, cujas colunas representam os quatro critérios: latência estimada, ocupação de fila, taxa de canal e energia de transmissão. A partir de $\mathbf{X}$, o módulo gera o vetor de estado fixo $\mathbf{S}_t \in [0,1]^{15}$ que é entregue à MLP, a qual emite a ação binária $a_t \in \{0, 1\}$ em microssegundos.

A Figura~\ref{fig:arquitetura} ilustra o fluxo completo de informação entre os componentes.

\begin{figure}[htbp]
\centering
\resizebox{\linewidth}{!}{%
\begin{tikzpicture}[
    % ── Blocos principais ─────────────────────────────────────────────
    envblock/.style={rectangle, draw, rounded corners=4pt,
                     minimum width=2.6cm, minimum height=1.4cm,
                     align=center, font=\small,
                     fill=blue!7, draw=blue!55, line width=0.9pt},
    topblock/.style={rectangle, draw, rounded corners=4pt,
                     minimum width=2.9cm, minimum height=1.4cm,
                     align=center, font=\small,
                     fill=teal!8, draw=teal!55, line width=0.9pt},
    ppoblock/.style={rectangle, draw, rounded corners=4pt,
                     minimum width=2.8cm, minimum height=1.4cm,
                     align=center, font=\small,
                     fill=purple!7, draw=purple!50, line width=0.9pt},
    actblock/.style={rectangle, draw, rounded corners=4pt,
                     minimum width=2.4cm, minimum height=1.4cm,
                     align=center, font=\small,
                     fill=orange!7, draw=orange!55, line width=0.9pt},
    % ── Sub-blocos do triplete ────────────────────────────────────────
    triplet/.style={rectangle, draw, rounded corners=3pt,
                    minimum width=2.1cm, minimum height=1.0cm,
                    align=center, font=\scriptsize,
                    fill=teal!4, draw=teal!40, line width=0.6pt},
    % ── Setas ─────────────────────────────────────────────────────────
    arrow/.style={->, >=stealth, thick, draw=black!70},
    darrow/.style={->, >=stealth, thick, dashed, draw=black!55},
    tinyarrow/.style={->, >=stealth, draw=gray!60},
    lbl/.style={font=\scriptsize, text=black!65},
    tlbl/.style={font=\scriptsize, text=teal!70!black},
]

% ── Fluxo principal ──────────────────────────────────────────────────
\node[envblock] (env)
    {\textbf{Ambiente VEC}\\$N$ candidatos\\(RSUs, MEC, veículos)};

\node[topblock, right=1.6cm of env] (top)
    {\textbf{Módulo TOPSIS}\\$\mathbf{X}\!\in\!\mathbb{R}^{N\times4}$\\
     $\downarrow\;\mathbf{S}_t\!\in\![0,1]^{15}$};

\node[ppoblock, right=1.6cm of top] (ppo)
    {\textbf{Política $\pi_\theta$}\\MLP $[256,\!256]$\\
     \textit{\scriptsize (treinada pelo PPO)}};

\node[actblock, right=1.5cm of ppo] (act)
    {\textbf{Decisão}\\$a_t\!=\!0$\,(LOCAL)\\$a_t\!=\!1$\,(OFFLOAD)};

% Setas principais
\draw[arrow] (env.east)
    -- node[above, lbl]{$\mathbf{X}\!\in\!\mathbb{R}^{N\times4}$} (top.west);
\draw[arrow] (top.east)
    -- node[above, lbl]{$\mathbf{S}_t\!\in\![0,1]^{15}$} (ppo.west);
\draw[arrow] (ppo.east)
    -- node[above, lbl]{$a_t$} (act.west);

% Loop de recompensa
\draw[darrow] (act.south) -- ++(0,-3.2)
    -| (env.south);
\node[lbl, below=0.15cm of act] {recompensa $r_t$\;\textit{(treinamento)}};

% ── Expansão do triplete ─────────────────────────────────────────────
\node[triplet, below=1.9cm of top, xshift=-2.2cm] (sbest)
    {$\mathbf{s}_\textup{best}$\\
     $[C^*,\hat\tau,\hat{Q},\hat{C},\hat{E}]$};

\node[triplet, below=1.9cm of top] (sagent)
    {$\mathbf{s}_\textup{agent}$\\
     $[C^*,\hat\tau,\hat{Q},\hat{C},\hat{E}]$};

\node[triplet, below=1.9cm of top, xshift=+2.2cm] (sworst)
    {$\mathbf{s}_\textup{worst}$\\
     $[C^*,\hat\tau,\hat{Q},\hat{C},\hat{E}]$};

% Conectores TOPSIS → triplete
\draw[tinyarrow] (top.south) -- ++(0,-0.3) -| (sbest.north);
\draw[tinyarrow] (top.south) -- (sagent.north);
\draw[tinyarrow] (top.south) -- ++(0,-0.3) -| (sworst.north);

% Rótulos
\node[tlbl, above=0.04cm of sbest]  {melhor ($\uparrow C^*$)};
\node[tlbl, above=0.04cm of sagent] {agente (alvo)};
\node[tlbl, above=0.04cm of sworst] {pior ($\downarrow C^*$)};

% Equação de composição
\node[font=\small, text=black!55, below=0.3cm of sagent]
    {$\mathbf{S}_t =
      \bigl[\;\underbrace{\mathbf{s}_\textup{best}}_{5}
            \;\|\;
            \underbrace{\mathbf{s}_\textup{agent}}_{5}
            \;\|\;
            \underbrace{\mathbf{s}_\textup{worst}}_{5}\;\bigr]
      \in[0,1]^{15}$};

\end{tikzpicture}%
}% end resizebox
\caption{Arquitetura da solução proposta. O Ambiente \gls{VEC} expõe uma matriz de decisão $\mathbf{X}\!\in\!\mathbb{R}^{N\times4}$ com $N$ candidatos variável (RSUs, servidores MEC ou veículos vizinhos); as 4 colunas correspondem a latência estimada, ocupação de fila, taxa de canal e energia de transmissão. O Módulo \gls{TOPSIS} reduz essa entrada a um vetor fixo $\mathbf{S}_t\!\in\![0,1]^{15}$ via triplete \{melhor, agente, pior\}, cada nó com cinco atributos normalizados $(C^*, \hat\tau, \hat{Q}, \hat{C}, \hat{E})$. A Política $\pi_\theta$ — uma MLP $[256{\times}256]$ \emph{treinada} pelo algoritmo \gls{PPO} — recebe $\mathbf{S}_t$ e emite a ação binária $a_t$ em tempo real. Durante o treinamento (linha tracejada), a ação retroalimenta o simulador com a recompensa $r_t$; em implantação, apenas a MLP opera.}
\label{fig:arquitetura}
\end{figure}

% ============================================================
\section{Formulação Formal do Problema}
\label{sec:formulacao-formal}
% ============================================================

Seja um veículo agente operando em uma área de cobertura com $N_s$ nós de infraestrutura heterogêneos (e.g., \glspl{RSU}, servidores \gls{MEC}, \glspl{gNB} \gls{5G}) e $N_v - 1$ veículos vizinhos dentro do alcance de comunicação. A cada instante $t$, o veículo agente gera uma tarefa com tamanho $b_t$ (em bytes), densidade computacional $\rho_t$ (ciclos/byte) e prazo $D_t$ (em segundos). O veículo deve decidir se executa a tarefa \emph{localmente} (no próprio veículo), ou se a \emph{descarrega} para um dos $N = N_s + (N_v - 1)$ candidatos disponíveis, que incluem tanto servidores de infraestrutura (V2I) quanto veículos vizinhos (V2V). O objetivo central é:

\begin{equation}
    \max_{\pi} \;\mathbb{E}_\pi\!\left[\sum_{t=0}^{T} \mathbf{1}\!\left[\tau_t \leq D_t\right]\right]
    \quad \text{s.a.} \quad \mathbb{E}_\pi\!\left[E_t\right] \leq E_{\max},
    \label{eq:objetivo-global}
\end{equation}

\noindent onde $\tau_t$ é a latência total da execução da tarefa $t$, $D_t$ é o prazo específico da tarefa e $E_t$ é a energia consumida. O problema é formulado rigorosamente como um \textbf{\gls{POMDP}}, definido pela sétupla $(\mathcal{S}, \mathcal{A}, \Omega, \mathcal{P}, \mathcal{O}, \mathcal{R}, \gamma)$:

\begin{itemize}
    \item \textbf{Espaço de estados latentes}: $\mathcal{S}$, que contém o estado completo e não observável da rede — taxas de canal instantâneas de todos os nós, estados internos das filas de todos os nós (servidores e veículos) e posições exatas dos veículos. O agente \textbf{nunca observa} $\mathbf{s}_t \in \mathcal{S}$ diretamente.

    \item \textbf{Espaço de ações}: $\mathcal{A} = \{0, 1\}$, onde $a_t = 0$ indica execução local (\texttt{LOCAL}) e $a_t = 1$ indica descarregamento (\texttt{OFFLOAD}) para o nó — servidor de infraestrutura ou veículo vizinho — de maior $C^*$ no ranqueamento \gls{TOPSIS}.

    \item \textbf{Espaço de observações}: $\Omega \subseteq [0,1]^{15}$. O agente recebe a observação $o_t \in \Omega$, que é o vetor de estado fixo $\mathbf{S}_t$ construído pelo módulo \gls{TOPSIS} (Seções~\ref{sec:topsis} e~\ref{sec:estado-fixo}).

    \item \textbf{Função de transição}: $\mathcal{P}(s_{t+1} \mid s_t, a_t)$, não-estacionária devido à mobilidade dos veículos, às flutuações de canal e à cadeia de Markov de regimes de tráfego (Seção~\ref{sec:modelo-ambiente}).

    \item \textbf{Função de observação}: $\mathcal{O}(o_t \mid s_t, a_{t-1})$, que mapeia o estado latente $s_t$ e a ação anterior $a_{t-1}$ para a observação $o_t \in \Omega$. O módulo \gls{TOPSIS} implementa esta função: a partir das medições ruidosas disponíveis (estimativas de canal e fila), produz a observação $\mathbf{S}_t \in [0,1]^{15}$.

    \item \textbf{Função de recompensa}: $\mathcal{R}(s_t, a_t) = r_t$, definida em detalhes na Seção~\ref{sec:recompensa}.

    \item \textbf{Fator de desconto}: $\gamma = 0{,}99$, discutido na Seção~\ref{sec:recompensa}.
\end{itemize}

\subsection{Justificativa do \glsentryshort{POMDP}}
\label{subsec:pomdp-justificativa}

O estado latente $s_t \in \mathcal{S}$ é inacessível por três razões estruturais. Primeiro, taxas de canal \gls{V2I} são estocásticas e variam com a posição do veículo — o agente observa apenas estimativas obtidas com atraso de um passo de tempo, sujeitas a ruído de sondagem. Segundo, o estado interno das filas dos servidores remotos é observado indiretamente, via telemetria com latência de propagação. Terceiro, posições dos veículos evoluem continuamente segundo um processo estocástico, introduzindo incerteza nas estimativas de distância e, portanto, de latência e energia \cite{uddin2025, farimani2024}. O módulo \gls{TOPSIS} implementa a função de observação $\mathcal{O}$: transforma as medições parciais disponíveis em $o_t = \mathbf{S}_t \in [0,1]^{15}$, resumindo o estado latente em uma representação de dimensão fixa que captura a melhor opção de offload, o custo local e a dispersão do campo de candidatos.

% ============================================================
\section{Modelagem do Ambiente de Simulação}
\label{sec:modelo-ambiente}
% ============================================================

O ambiente de simulação foi implementado como um ambiente \textit{gymnasium} customizado, seguindo a interface padrão de \gls{RL}. Esta seção detalha cada componente do modelo.

% ------------------------------------------------------------
\subsection{Topologia e Mobilidade}
\label{subsec:topologia}
% ------------------------------------------------------------

Considera-se uma área quadrada de dimensão $A \times A$ metros, com $N_v$ veículos e $N_s$ servidores de borda posicionados no interior da área. Os veículos se movem com velocidade aleatória uniforme, refletindo nas bordas da área (modelo de reflexão), de forma a modelar um cenário urbano com tráfego persistente.

A capacidade de processamento local do veículo segue um \textbf{processo \gls{AR}(1)} (autorregressivo de primeira ordem), que modela a autocorrelação temporal da disponibilidade de \gls{CPU}:

\begin{equation}
    f_t = f_\mu + \rho\,(f_{t-1} - f_\mu) + \sigma_\varepsilon\,\varepsilon_t, \quad \varepsilon_t \sim \mathcal{N}(0,1),
    \label{eq:ar1-cpu}
\end{equation}

\noindent com parâmetros $f_\mu = 1{,}5$ GHz (média de longo prazo), $\rho = 0{,}95$ (coeficiente de autocorrelação), $\sigma_\varepsilon = 0{,}2$ GHz (desvio padrão do ruído), e $f_t$ limitado ao intervalo $[0{,}5;\;4{,}0]$ GHz. A interpretação do \gls{AR}(1) é direta: o parâmetro $\rho$ próximo de 1 garante persistência temporal — uma \gls{CPU} congestionada no instante $t$ tende a permanecer congestionada em $t+1$, o que é consistente com a realidade de processos de sistema operacional que acumulam carga e a liberam gradualmente. O ruído $\varepsilon_t$ introduz variabilidade para evitar trajetórias perfeitamente determinísticas.

% ------------------------------------------------------------
\subsection{Heterogeneidade dos Servidores}
\label{subsec:servidores}
% ------------------------------------------------------------

Os $N_s$ servidores de borda possuem capacidades de processamento heterogêneas, ciclando pelo conjunto $\{1{,}0;\;2{,}0;\;4{,}0\}$ GHz de forma a cobrir toda a faixa de variação para qualquer valor de $N_s$. Para $N_s = 3$, tem-se exatamente um servidor lento ($1{,}0$ GHz), um servidor médio ($2{,}0$ GHz) e um servidor rápido ($4{,}0$ GHz), representando a heterogeneidade típica de \glspl{RSU} urbanas com diferentes gerações de hardware implantadas.

A heterogeneidade é uma característica de projeto deliberada e essencial. Em um cenário homogêneo (todos os servidores equivalentes), qualquer política de descarregamento que selecione um servidor arbitrário é ótima em termos de capacidade de processamento, tornando o problema trivial. A introdução de servidores com capacidades distintas, combinada com ocupações de fila diferentes e distâncias variáveis, cria um problema de seleção genuinamente não trivial em que a escolha do servidor errado pode resultar em violação de prazo, mesmo quando a decisão de descarregar é correta \cite{moghaddasi2024}.

% ------------------------------------------------------------
\subsection{Modelo de Filas}
\label{subsec:filas}
% ------------------------------------------------------------

Cada nó $i$ (veículo ou servidor) mantém uma fila discreta $Q_i \in \{0, 1, \ldots, Q_{\max}\}$, com $Q_{\max} = 8$. A dinâmica da fila segue o modelo:

\begin{equation}
    Q_i(t+1) = \min\!\bigl(Q_i(t) + \mathbf{1}[\text{nó $i$ selecionado em $t$}],\; Q_{\max}\bigr),
    \label{eq:fila-incremento}
\end{equation}

\noindent e, a cada passo de tempo, a fila drena com probabilidade $p_{\text{drain},i}$:

\begin{equation}
    Q_i(t+1) \leftarrow \max\!\bigl(Q_i(t+1) - 1,\; 0\bigr) \quad \text{com prob. } p_{\text{drain},i}.
    \label{eq:fila-dreno}
\end{equation}

A probabilidade de dreno é proporcional à capacidade de processamento do nó. Para servidores:
\begin{equation}
    p_{\text{drain},i}^{\text{server}} = 0{,}25 + 0{,}15 \cdot \frac{f_i}{4\;\text{GHz}},
    \label{eq:pdrain-server}
\end{equation}

\noindent resultando em $p_{\text{drain}} \in [0{,}25;\;0{,}40]$ (0,25 para servidor de 1 GHz, 0,40 para servidor de 4 GHz). Para o veículo local: $p_{\text{drain}}^{\text{vehicle}} = 0{,}20$. Este modelo aproxima o comportamento de uma fila M/M/1, onde o tempo médio de serviço é inversamente proporcional à capacidade de processamento, justificando a dependência linear entre $p_{\text{drain}}$ e $f_i$.

A carga efetiva do nó é calculada como:
\begin{equation}
    \rho_i = \frac{Q_i}{Q_{\max}}.
    \label{eq:carga-efetiva}
\end{equation}

% ------------------------------------------------------------
\subsection{Perfis de Tarefas e Regimes de Tráfego}
\label{subsec:regimes}
% ------------------------------------------------------------

O ambiente define três \textbf{regimes de tráfego}, cada um com características distintas de tamanho, complexidade e prazo de tarefa:

\begin{table}[htbp]
\centering
\caption{Parâmetros dos regimes de tráfego por tipo de tarefa.}
\label{tab:regimes-trafego}
\begin{tabular}{lccc}
\hline
\textbf{Regime} & \textbf{Tamanho} $b$ (KB) & \textbf{Densidade} $\rho$ (ciclos/byte) & \textbf{Prazo} $D$ (ms) \\
\hline
Leve (\textit{light})       & $\mathcal{U}[5,\;15]$   & 7\,000  & $\mathcal{U}[80,\;150]$  \\
Crítico (\textit{critical})  & $\mathcal{U}[10,\;20]$  & 9\,000  & $\mathcal{U}[50,\;100]$  \\
Pesado (\textit{heavy})      & $\mathcal{U}[50,\;80]$  & 11\,000 & $\mathcal{U}[250,\;500]$ \\
\hline
\end{tabular}
\end{table}

A transição entre regimes segue uma \textbf{cadeia de Markov} com matriz de transição:

\begin{equation}
    \mathbf{\Pi} =
    \begin{pmatrix}
        0{,}70 & 0{,}20 & 0{,}10 \\
        0{,}20 & 0{,}60 & 0{,}20 \\
        0{,}10 & 0{,}20 & 0{,}70
    \end{pmatrix},
    \label{eq:matriz-transicao}
\end{equation}

\noindent onde as linhas e colunas correspondem, respectivamente, aos estados \textit{light}, \textit{critical} e \textit{heavy}. A diagonal dominante (0,70 e 0,60) reflete a \textbf{persistência temporal} dos regimes: uma vez no regime leve, a rede permanece nele com probabilidade 0,70, o que cria não-estacionariedade temporal. O agente deve aprender políticas robustas que funcionem bem em todos os três regimes, sem identificação explícita do regime corrente.

% ------------------------------------------------------------
\subsection{Modelos de Canal}
\label{subsec:canal}
% ------------------------------------------------------------

O cálculo da taxa de dados segue um pipeline de três etapas para ambos os tipos de enlace: (i)~perda de percurso, (ii)~SNR e (iii)~capacidade de Shannon com fator de eficiência espectral.

\paragraph{Canal \gls{V2I} (3GPP TR 38.901 UMi Street Canyon).}
A perda de percurso entre o veículo e um nó de infraestrutura distingue condições de visada direta (\gls{LOS}) e sem visada (\gls{NLOS}). A probabilidade de \gls{LOS} a distância $d$ é dada pela Tabela~B.1.2.1-1 do TR 38.901:

\begin{equation}
    p_{\mathrm{LOS}}(d) = \min\!\left(\frac{18}{d},\,1\right)\!\left(1 - e^{-d/36}\right) + e^{-d/36}.
    \label{eq:plos}
\end{equation}

As perdas de percurso para cada condição são, com $f_c = 3{,}5$ GHz:

\begin{align}
    \mathrm{PL}^{\mathrm{LOS}}(d)  &= 32{,}4 + 21\log_{10}(d) + 20\log_{10}(f_c/10^9)\;\text{[dB]},
    \label{eq:pl-los} \\
    \mathrm{PL}^{\mathrm{NLOS}}(d) &= \max\!\Bigl(35{,}3\log_{10}(d) + 22{,}4 + 21{,}3\log_{10}(f_c/10^9),\;
                                       \mathrm{PL}^{\mathrm{LOS}}(d)\Bigr)\;\text{[dB]}.
    \label{eq:pl-nlos}
\end{align}

A perda efetiva é a média ponderada pela probabilidade de \gls{LOS}:

\begin{equation}
    \mathrm{PL}_{V2I}(d) = p_{\mathrm{LOS}}(d)\cdot\mathrm{PL}^{\mathrm{LOS}}(d)
                         + \bigl(1 - p_{\mathrm{LOS}}(d)\bigr)\cdot\mathrm{PL}^{\mathrm{NLOS}}(d).
    \label{eq:pathloss-v2i}
\end{equation}

O piso de ruído térmico é calculado explicitamente a partir da largura de banda $B = 40$ MHz e da figura de ruído do receptor $NF = 7$ dB:

\begin{equation}
    N_0 = -174\;\text{dBm/Hz} + 10\log_{10}(B) + NF = {-91}\;\text{dBm}.
    \label{eq:noise-floor}
\end{equation}

A \gls{SNR} recebida e a taxa de dados resultante são:

\begin{align}
    \mathrm{SNR}_{V2I}(d) &= P_{tx} - \mathrm{PL}_{V2I}(d) - N_0,
    \label{eq:snr-v2i} \\
    C_{V2I}(d) &= \eta \cdot B \cdot \log_2\!\left(1 + 10^{\,\mathrm{SNR}_{V2I}(d)/10}\right),
    \label{eq:capacidade-v2i}
\end{align}

\noindent com potência de transmissão $P_{tx} = 23$ dBm e fator de eficiência espectral $\eta = 0{,}6$, que modela o overhead de protocolos, retransmissões \gls{HARQ} e bits de controle. Como referência: a 50~m, $\mathrm{SNR} \approx +27{,}8$ dB e $C \approx 222$ Mbps; a 200~m, $\mathrm{SNR} \approx +1{,}0$ dB e $C \approx 28$ Mbps.

\paragraph{Canal \gls{V2V} (3GPP TR 37.885 C-V2X Sidelink).}
Para comunicação entre veículos, o TR 37.885 especifica um modelo de caminho único sem distinção LOS/NLOS, com $f_c = 5{,}9$ GHz:

\begin{equation}
    \mathrm{PL}_{V2V}(d) = 38{,}77 + 16{,}7\log_{10}(d) + 18{,}2\log_{10}(f_c/10^9)\;\text{[dB]}.
    \label{eq:pathloss-v2v}
\end{equation}

Com $B = 20$ MHz, $NF = 9$ dB e $P_{tx} = 23$ dBm, o piso de ruído é $N_0 = {-92}$ dBm, e a taxa de dados segue a mesma expressão de Shannon (Equação~\eqref{eq:capacidade-v2i}) com os parâmetros V2V. Como referência: a 30~m, $C \approx 150$ Mbps; a 100~m, $C \approx 115$ Mbps — a variação é menor que no V2I pois o expoente de distância (16,7 vs 21–35,3) é mais baixo, refletindo a geometria de propagação lateral típica do enlace veicular.

% ------------------------------------------------------------
\subsection{Estimativa de Latência}
\label{subsec:latencia}
% ------------------------------------------------------------

\paragraph{Descarregamento (OFFLOAD).} Dado que o nó selecionado (servidor de infraestrutura ou veículo vizinho) possui frequência $f_s$ e carga $\rho_s = Q_s/Q_{\max}$, a latência total de descarregamento é:

\begin{align}
    \tau_{tx}      &= \frac{b \cdot 8}{C(d_s)}, \label{eq:tau-tx} \\
    \tau_{proc}    &= \frac{b \cdot \rho_{\text{dens}}}{f_s}, \label{eq:tau-proc} \\
    \tau_{queue}   &= \frac{\rho_s}{1 - \rho_s + \varepsilon} \cdot \tau_{proc}, \label{eq:tau-queue} \\
    \tau_{offload} &= \tau_{tx} + \tau_{proc} + \tau_{queue}, \label{eq:tau-offload}
\end{align}

\noindent onde $b$ é o tamanho da tarefa em bytes, $\rho_{\text{dens}}$ é a densidade computacional em ciclos/byte, $C(d_s)$ é a capacidade do canal ao nó escolhido (V2I para servidores, V2V para veículos vizinhos), e $\varepsilon = 10^{-6}$ previne divisão por zero quando $\rho_s \to 1$.

\paragraph{Execução local (LOCAL).} Para execução no próprio veículo com frequência $f_v$ e carga $\rho_v$:

\begin{equation}
    \tau_{local} = \left(\frac{\rho_v}{1 - \rho_v + \varepsilon} + 1\right) \cdot \frac{b \cdot \rho_{\text{dens}}}{f_v}.
    \label{eq:tau-local}
\end{equation}

% ------------------------------------------------------------
\subsection{Estimativa de Energia}
\label{subsec:energia}
% ------------------------------------------------------------

\paragraph{Energia de transmissão (OFFLOAD).} Assumindo potência de transmissão constante $P_{tx}^{(W)} = 0{,}1$ W:

\begin{equation}
    E_{tx} = P_{tx}^{(W)} \cdot \tau_{tx}.
    \label{eq:energia-tx}
\end{equation}

\paragraph{Energia de processamento local (LOCAL).} O modelo cúbico de energia dinâmica de \gls{CPU} é amplamente adotado na literatura \cite{chen2025}:

\begin{equation}
    E_{local} = \kappa \cdot f_v^2 \cdot (b \cdot \rho_{\text{dens}}),
    \label{eq:energia-local}
\end{equation}

\noindent onde $\kappa = 10^{-28}$ é o coeficiente de capacitância efetiva. O fator $f_v^2$ (quadrático na frequência) captura o custo energético da tensão de operação, enquanto o fator $b \cdot \rho_{\text{dens}}$ representa o total de ciclos de \gls{CPU} necessários.

% ============================================================
\section{Tomada de Decisão Multicritério: TOPSIS}
\label{sec:topsis}
% ============================================================

% ------------------------------------------------------------
\subsection{Motivação para o Uso do TOPSIS na Construção do Estado}
\label{subsec:topsis-motivacao}
% ------------------------------------------------------------

O descarregamento de tarefas em ambientes \gls{VEC} é intrinsecamente um problema de \textbf{\gls{MCDM}}: existem múltiplos critérios conflitantes — latência de transmissão, ocupação de fila, taxa de dados e consumo de energia — para os quais não existe uma solução única ótima em todos simultaneamente \cite{miriyala2025}. Minimizar a latência pode exigir o servidor mais próximo, que frequentemente é o mais congestionado; maximizar a taxa de dados pode requerer maior potência de transmissão, elevando o consumo energético. A ausência de uma solução Pareto-dominante universal justifica o emprego de um método \gls{MCDM} capaz de agregar os critérios em um escore composto.

A escolha do \gls{TOPSIS} é motivada por três propriedades que o tornam particularmente adequado como pré-processador de estado para redes neurais:

\begin{enumerate}
    \item \textbf{Escore naturalmente normalizado.} O coeficiente de proximidade $C^*_i \in [0,1]$ é produzido diretamente pelo algoritmo, sem necessidade de pré-processamento adicional antes da entrada na rede neural. A normalização decorre da própria definição de $C^*_i$ como razão de distâncias (Equação~\eqref{eq:closeness}).

    \item \textbf{Estatística suficiente de ordem relativa.} O escore $C^*_i$ captura toda a informação de ranqueamento dos $N$ candidatos em uma única dimensão interpretável, sem expor à rede neural a dimensionalidade bruta $N \times 4$ da matriz de decisão. Formalmente, $C^*_i$ é uma função suficiente para a ordenação dos candidatos em relação aos quatro critérios ponderados: qualquer política ótima que dependa apenas da ordenação dos candidatos pode ser expressa como função de $\{C^*_i\}_{i=1}^N$.

    \item \textbf{Complexidade favorável e independência de $N$.} O \gls{TOPSIS} opera em $O(N \log N)$, dominado pela ordenação dos escores. Métodos baseados em grafos (\glspl{GNN}) têm complexidade $O(N^2 \cdot D_{\text{GNN}})$ por camada de passagem de mensagem, crescendo quadraticamente com o número de candidatos e exigindo retreinamento completo para cada novo valor de $N$. O \gls{TOPSIS}, combinado com a representação triplet (Seção~\ref{sec:estado-fixo}), produz um estado de dimensão fixa $[0,1]^{15}$ independente de $N$ — propriedade fundamental para a generalização zero-shot da política (Seção~\ref{subsec:zeroshot}).
\end{enumerate}

Alternativas como a soma ponderada simples (\textit{weighted sum}) perdem a informação de dominância de Pareto: uma alternativa que domina todas as outras em três dos quatro critérios, mas é levemente inferior no quarto, pode receber escore agregado inferior a uma alternativa mediocre em todos os critérios. O \gls{TOPSIS} preserva esta estrutura ao considerar simultaneamente a proximidade à solução ideal positiva e a distância à solução ideal negativa.

\paragraph{Justificativa dos pesos $\mathbf{w} = [0{,}35;\;0{,}25;\;0{,}25;\;0{,}15]$.}

A distribuição de pesos é motivada pela hierarquia de requisitos \gls{URLLC} estabelecida pelo 3GPP: latência é o critério primário de qualidade de serviço em sistemas de comunicação ultrafidedigna, com requisitos tipicamente inferiores a 1--10 ms para aplicações críticas \cite{uddin2025}. A justificativa para cada peso é:

\begin{itemize}
    \item $w_\tau = 0{,}35$ (latência): peso majoritário, refletindo a primazia da latência em cenários \gls{URLLC}. A latência de transmissão e processamento determina diretamente o cumprimento ou violação do prazo $D_t$.
    \item $w_\rho = 0{,}25$ (ocupação de fila): peso equivalente à taxa de dados, pois a ocupação de fila determina a latência de espera $\tau_{queue}$ (Equação~\eqref{eq:tau-queue}), que é um componente direto da latência total — tornando-a funcionalmente equivalente em importância à latência de transmissão.
    \item $w_C = 0{,}25$ (taxa de dados): a taxa de canal determina $\tau_{tx}$ diretamente (Equação~\eqref{eq:tau-tx}). A equivalência funcional entre $w_\rho$ e $w_C$ reflete o fato de que ambos os critérios contribuem de forma comparável para a latência total em cenários \gls{VEC} com distâncias típicas de 50--200 m.
    \item $w_E = 0{,}15$ (energia): peso minoritário, pois a energia afeta a vida da bateria do veículo, mas não compromete imediatamente o cumprimento de prazo — constituindo um objetivo secundário no contexto \gls{URLLC}.
\end{itemize}

\paragraph{Justificativa da norma $L_2$ (Euclidiana) no Passo 4.}

A norma $L_2$ é a métrica original do \gls{TOPSIS}, introduzida por \citeonline{hwang1981} e mantida como padrão na literatura de \gls{MCDM} \cite{miriyala2025}. A propriedade fundamental que justifica esta escolha no presente contexto é que a norma $L_2$ penaliza candidatos que se desviam simultaneamente em vários critérios do ideal de forma mais severa do que a norma $L_1$: ao elevar os desvios ao quadrado antes de somá-los, ela atribui custo superlinear a afastamentos múltiplos e simultâneos. Esta propriedade é adequada para o problema de descarregamento, onde um candidato com latência alta \textit{e} fila cheia \textit{e} baixa taxa de dados deve receber uma penalidade mais severa do que um candidato com um único critério ruim — comportamento que a $L_2$ captura naturalmente após a normalização vetorial do Passo 1.

% ============================================================
\section{Representação de Estado Invariante à Escala}
\label{sec:estado-fixo}
% ============================================================

Um desafio central em \gls{RL} para redes \gls{VEC} é a \textbf{variabilidade do número de candidatos}: conforme veículos e servidores entram e saem da área de cobertura, $N$ pode variar de 1 a dezenas. Arquiteturas como \glspl{GNN} lidam com topologias variáveis ao custo de elevada complexidade computacional e dificuldade de generalização \cite{miriyala2025}. A abordagem proposta resolve este problema por meio de uma representação de estado de dimensão fixa, construída pelo \gls{TOPSIS} e independente de $N$.

% ------------------------------------------------------------
\subsection{Características por Nó}
\label{subsec:features-por-no}
% ------------------------------------------------------------

Após a execução do \gls{TOPSIS}, cada candidato $i$ é representado por um vetor de características de 5 dimensões, todas normalizadas para o intervalo $[0,1]$:

\begin{equation}
    \mathbf{s}_i = \left[C^*_i,\;\frac{\tau_i}{\tau_{\max}},\;\frac{Q_i}{Q_{\max}},\;\frac{C_i}{C_{\max}},\;\frac{E_i}{E_{\max}}\right],
    \label{eq:feature-vector}
\end{equation}

\noindent onde cada componente cumpre um papel informacional específico e insubstituível para a política do agente:

\begin{itemize}
    \item $C^*_i \in [0,1]$: escore \gls{TOPSIS} composto — sumariza os quatro critérios em uma única dimensão interpretável pela rede neural como ``quão boa é esta opção em termos relativos ao conjunto de candidatos disponíveis''. A justificativa para incluir $C^*_i$ como feature explícita, e não apenas como critério de seleção, é que ele fornece ao crítico uma representação comprimida da posição relativa do candidato, facilitando a aprendizagem da função de valor $V^\phi(\mathbf{S}_t)$.

    \item $\tau_i / \tau_{\max}$: a latência absoluta normalizada informa a viabilidade do prazo em escala absoluta. O escore $C^*_i$ é uma medida relativa — o melhor dos $N$ candidatos terá $C^*_i \to 1$ mesmo que todos os candidatos sejam objetivamente ruins. A latência normalizada $\tau_i/\tau_{\max}$ fornece o sinal de escala absoluta necessário para que o agente distinga um ambiente favorável (onde todos os candidatos têm baixa latência) de um ambiente desfavorável (onde todos os candidatos violam o prazo) — informação que $C^*_i$ sozinho não pode fornecer.

    \item $Q_i / Q_{\max}$: prediz o custo futuro de offloading para o nó $i$. Uma fila alta no instante $t$ sinaliza que offloads futuros para este nó serão penalizados pela componente $\tau_{queue}$ (Equação~\eqref{eq:tau-queue}). A propriedade AR(1) das filas ($\rho = 0{,}95$) garante persistência temporal: o nível de fila atual é um preditor relevante do nível futuro, tornando $Q_i/Q_{\max}$ uma feature de valor para o crítico ao estimar retornos de longo prazo $V^\phi(\mathbf{S}_t)$.

    \item $C_i / C_{\max}$: a taxa de dados determina $\tau_{tx}$ diretamente (Equação~\eqref{eq:tau-tx}), sendo necessária para o crítico prever como quedas de canal em futuros passos afetarão as recompensas. Flutuações de canal em ambientes \gls{V2I} são autocorrelacionadas com a posição do veículo — incluir $C_i/C_{\max}$ permite ao crítico antecipar degradação de canal e ajustar as estimativas de valor correspondentemente.

    \item $E_i / E_{\max}$: a energia normalizada aparece explicitamente na função de recompensa $r_t$ (via $J_t$, Equação~\eqref{eq:custo-conjunto}). A sua inclusão no vetor de estado é necessária para que o crítico aprenda $V^\phi(\mathbf{S}_t)$ com precisão: sem $E_i/E_{\max}$, o crítico não teria acesso direto à componente de energia da recompensa, forçando-o a inferi-la indiretamente a partir de $C_i/C_{\max}$ e $\tau_i/\tau_{\max}$ — o que introduziria viés nas estimativas de vantagem $\hat{A}_t^{\text{GAE}}$.
\end{itemize}

\paragraph{Suficiência da representação: por que $[C^*_i,\,\hat\tau_i]$ não basta.}
\label{par:suficiencia}
Uma pergunta natural é se uma representação mínima $\mathbf{s}_i = [C^*_i,\,\hat\tau_i]$ — apenas o escore relativo e a latência absoluta — seria suficiente para um bom aprendizado e generalização. A resposta é negativa, e o argumento é estrutural.

Para a \textbf{decisão instantânea} em $t$, $[C^*, \hat\tau]$ é razoavelmente informativo: $C^*_i$ já agrega os quatro critérios, e $\hat\tau_i$ ancora a escala absoluta. O problema não está na decisão em $t$, mas na \textbf{estimação do valor pelo crítico} $V^\phi(\mathbf{S}_t) = \mathbb{E}\!\left[\sum_{k \geq 0} \gamma^k r_{t+k}\right]$.

Considere dois candidatos com $C^* = 0{,}70$ e $\hat\tau = 0{,}15$ — estados idênticos em $[C^*, \hat\tau]$, mas opostos em dinâmica futura:

\begin{center}
\small
\begin{tabular}{lccl}
\toprule
Candidato & $\hat{Q}$ & Trajetória futura & Ação ótima \\
\midrule
Servidor rápido, fila crescendo & 0,80 & $\tau_{t+1}$ duplica (fila AR(1)) & LOCAL \\
Servidor médio, fila drenando   & 0,20 & $\tau_{t+1}$ cai                   & OFFLOAD \\
\bottomrule
\end{tabular}
\end{center}

\noindent Sem $\hat{Q}_i$, esses dois estados são \textbf{indistinguíveis} para o crítico, que produzirá a mesma estimativa de valor para situações com retornos futuros opostos — introduzindo variância sistemática nas estimativas de vantagem $\hat{A}_t^{\text{GAE}}$ e degradando a convergência do PPO.

Analogamente, sem $\hat{E}_i$, o componente energético da recompensa $r_t = -[0{,}7\,\hat\tau + 0{,}3\,\hat{E}]$ torna-se \textbf{invisível para o crítico}: $30\%$ do sinal de recompensa contínua não é observado, introduzindo viés nas estimativas de $V^\phi$. Sem $\hat{C}_i$, o crítico não consegue antecipar degradação de canal entre passos consecutivos — especialmente crítico no regime heavy, quando o veículo se afasta da RSU e o canal deteriora rapidamente.

Em síntese: $[C^*, \hat\tau]$ seria suficiente apenas em um ambiente \textbf{estacionário e sem filas}, onde a decisão é um problema de seleção \textit{one-shot} sem dinâmica temporal. No ambiente \gls{VEC} com AR(1), cadeia de Markov de regimes e filas acopladas, os três features adicionais $[\hat{Q}, \hat{C}, \hat{E}]$ são necessários para que o crítico estime $V^\phi(\mathbf{S}_t)$ com precisão suficiente para que o PPO convirja de forma confiável. A Tabela~\ref{tab:suficiencia} sumariza esta análise.

\begin{table}[htbp]
\centering
\caption{Análise de suficiência das representações de estado. A coluna ``Decisão'' avalia a qualidade da ação em $t$; ``Aprendizado'' avalia a precisão do crítico ao longo do treinamento.}
\label{tab:suficiencia}
\small
\begin{tabular}{p{3.8cm} p{2.6cm} p{2.6cm} p{4.2cm}}
\toprule
\textbf{Representação} & \textbf{Decisão em $t$} & \textbf{Aprendizado} & \textbf{Limitação principal} \\
\midrule
$[C^*_i,\,\hat\tau_i]$ — 6D
  & Razoável
  & Comprometido
  & Crítico cego à dinâmica de fila/canal/energia; alta variância em $\hat{A}_t$ \\
\addlinespace
$[C^*_i,\,\hat\tau_i,\,\hat{Q}_i]$ — 9D
  & Boa
  & Melhorado
  & Sem sinal de energia (30\% da recompensa invisível) \\
\addlinespace
$[C^*_i,\,\hat\tau_i,\,\hat{Q}_i,\,\hat{C}_i,\,\hat{E}_i]$ — 15D
  & Ótima
  & Estável
  & Representação minimal completa \\
\bottomrule
\end{tabular}
\end{table}

Para o veículo local (agente), o vetor $\mathbf{s}_{\text{agent}}$ é calculado de forma análoga, com a latência local estimada pela Equação~\eqref{eq:tau-local}, a ocupação de fila local, taxa de dados de uplink nula ($C_i = 0$ pois não há transmissão no modo local) e energia pelo modelo cúbico (Equação~\eqref{eq:energia-local}).

% ------------------------------------------------------------
\subsection{Construção do Estado Fixo $[\mathbf{s}_{\text{best}} \mid \mathbf{s}_{\text{agent}} \mid \mathbf{s}_{\text{worst}}]$}
\label{subsec:estado-triplet}
% ------------------------------------------------------------

Dados $N \geq 1$ candidatos ranqueados pelo \gls{TOPSIS} em ordem decrescente de $C^*$, o estado fixo de 15 dimensões é:

\begin{equation}
    \mathbf{S}_t = \left[\mathbf{s}_{\text{rank}_1} \;\middle|\; \mathbf{s}_{\text{agent}} \;\middle|\; \mathbf{s}_{\text{rank}_N}\right] \in [0,1]^{15},
    \label{eq:estado-fixo}
\end{equation}

\noindent onde $\mathbf{s}_{\text{rank}_1}$ é o vetor do candidato de maior $C^*$ (melhor opção de descarregamento) e $\mathbf{s}_{\text{rank}_N}$ é o vetor do candidato de menor $C^*$ (pior opção disponível). O estado $\mathbf{S}_t$ possui \textbf{sempre 15 dimensões}, independentemente do valor de $N$.

A escolha do triplet $[\mathbf{s}_{\text{best}} \mid \mathbf{s}_{\text{agent}} \mid \mathbf{s}_{\text{worst}}]$ é motivada pela \textbf{teoria da informação de decisão}: o estado ótimo minimal para uma decisão binária (offload vs local) deve responder a três perguntas fundamentais:

\begin{enumerate}
    \item \textit{Qual a melhor opção de offload disponível?} — respondida por $\mathbf{s}_{\text{best}}$ (rank 1).
    \item \textit{Qual o custo da alternativa local (não fazer offload)?} — respondida por $\mathbf{s}_{\text{agent}}$.
    \item \textit{Qual a dispersão do conjunto de candidatos?} — respondida por $\mathbf{s}_{\text{worst}}$ (rank $N$).
\end{enumerate}

Os três componentes cumprem papéis complementares e insubstituíveis:

\begin{enumerate}
    \item \textbf{$\mathbf{s}_{\text{best}}$ (rank 1):} Informa o agente sobre a melhor opção de offload disponível em $t$. A sua presença é condição necessária para que o agente avalie se descarregar é vantajoso em termos absolutos. Sem $\mathbf{s}_{\text{best}}$, o agente não tem acesso à melhor alternativa de offload disponível.

    \item \textbf{$\mathbf{s}_{\text{agent}}$:} Representa o estado local do veículo — latência esperada de execução local, ocupação atual da fila local e custo energético local. A sua presença é condição necessária para que o agente compare o custo do offload com o custo da execução local. Sem $\mathbf{s}_{\text{agent}}$, o agente seria incapaz de realizar a comparação offload-vs-local: equivaleria a tomar uma decisão de compra sem conhecer o preço da alternativa.

    \item \textbf{$\mathbf{s}_{\text{worst}}$ (rank $N$):} Serve como referência de dispersão do campo de candidatos. A quantidade $C^*_{\text{best}} - C^*_{\text{worst}}$ grande indica \textbf{heterogeneidade alta} — há candidatos muito bons e muito ruins, e a escolha do servidor importa mais do que a decisão offload/local. Um valor pequeno indica \textbf{homogeneidade} — a decisão offload/local domina sobre a escolha do servidor. Sem $\mathbf{s}_{\text{worst}}$, o agente perde este sinal de dispersão e não consegue distinguir ambientes heterogêneos (onde a seleção do servidor é crítica) de ambientes homogêneos (onde qualquer servidor é equivalente).
\end{enumerate}

A Tabela~\ref{tab:ablacao-informacional} sistematiza o conteúdo informacional de cada variante do estado, identificando qual pergunta de decisão cada uma deixa sem resposta.

\begin{table}[htbp]
\centering
\caption{Análise de conteúdo informacional das variantes do estado. Cada linha indica quais perguntas de decisão a variante é capaz de responder ($\checkmark$) ou não ($\times$).}
\label{tab:ablacao-informacional}
\begin{tabular}{lccc}
\hline
\textbf{Variante} & \textbf{Melhor offload?} & \textbf{Custo local?} & \textbf{Dispersão?} \\
\hline
\texttt{best|agent|worst} (proposta)                          & $\checkmark$ & $\checkmark$ & $\checkmark$ \\
\texttt{top3}: $[\mathbf{s}_1 | \mathbf{s}_2 | \mathbf{s}_3]$  & $\checkmark$ & $\times$     & $\checkmark$ \\
\texttt{top2\_agent}: $[\mathbf{s}_1 | \mathbf{s}_2 | \mathbf{s}_{\text{agent}}]$ & $\checkmark$ & $\checkmark$ & $\times$ \\
\texttt{agent\_only}: $[\mathbf{s}_{\text{agent}} | \mathbf{0} | \mathbf{0}]$      & $\times$     & $\checkmark$ & $\times$ \\
\hline
\end{tabular}
\end{table}

A variante \texttt{top3} omite o custo local: o agente não possui referência para comparar offload com execução local, sendo forçado a inferir o custo local indiretamente — o que introduz ambiguidade na aprendizagem da fronteira de decisão. A variante \texttt{top2\_agent} omite a dispersão: o agente não consegue distinguir um campo heterogêneo (onde selecionar o melhor servidor é crítico) de um campo homogêneo (onde a decisão offload/local domina). A variante \texttt{agent\_only} omite completamente a informação de offload, tornando impossível a comparação direta entre as alternativas. Apenas o triplet proposto responde às três perguntas simultaneamente.

\paragraph{Invariância dimensional.} Para qualquer $N \in \{1, 2, \ldots, \infty\}$, o \gls{TOPSIS} computa os escores $C^*_i$ para todos os $N$ candidatos em $O(N \log N)$, e então seleciona três entradas representativas. A operação de seleção é $O(N)$ (min e max do vetor de escores), mas a saída é sempre $\mathbb{R}^{15}$. Portanto, \textbf{um modelo treinado com $N = 3$ pode ser avaliado em cenários com $N$ muito maior sem qualquer modificação na arquitetura da rede neural}, pois o espaço de observação é fixo em $\dim = 15$ e o mapeamento $N \to 15$ é definido pela regra de seleção do \gls{TOPSIS}. Esta propriedade é fundamental para a viabilidade prática da abordagem em redes dinâmicas.

% ------------------------------------------------------------
\subsection{Justificativa por Análise de Conteúdo Informacional}
\label{subsec:ablativo}
% ------------------------------------------------------------

A escolha do triplet $[\mathbf{s}_{\text{best}} \mid \mathbf{s}_{\text{agent}} \mid \mathbf{s}_{\text{worst}}]$ é validada teoricamente pela análise de conteúdo informacional de cada variante alternativa, conforme sintetizado na Tabela~\ref{tab:ablacao-informacional}. A justificativa é estrutural: cada um dos três componentes responde a uma pergunta de decisão irredutível, e a ausência de qualquer um deles implica em informação insuficiente para a tomada de decisão ótima \cite{baker2024}.

A propriedade de suficiência informacional do triplet pode ser enunciada formalmente: para uma decisão binária $a_t \in \{0,1\}$ com custo dependente simultaneamente do melhor offload disponível, do custo local e da dispersão dos candidatos, o estado $\mathbf{S}_t = [\mathbf{s}_{\text{best}} \mid \mathbf{s}_{\text{agent}} \mid \mathbf{s}_{\text{worst}}]$ é uma \textbf{representação suficiente} no sentido de que nenhuma das três perguntas de decisão fica sem resposta — condição que nenhuma das variantes alternativas satisfaz completamente.

% ------------------------------------------------------------
\subsection{Generalização Zero-Shot por Transferência Estrutural}
\label{subsec:zeroshot}
% ------------------------------------------------------------

A representação de estado de dimensão fixa viabiliza uma propriedade notável: a política treinada com $N = 3$ candidatos deverá generalizar para cenários com $N$ arbitrariamente maior \textbf{sem nenhum retreinamento}. A justificativa desta capacidade de \textbf{generalização zero-shot} é de natureza matemática, decorrente da estrutura do mapeamento \gls{TOPSIS}.

\paragraph{Argumento matemático.} O mapeamento $f: \mathbb{R}^{N \times 4} \to [0,1]^{15}$ é contínuo e surjetivo sobre $[0,1]^{15}$ para qualquer $N \geq 1$: para qualquer ponto $\mathbf{s} \in [0,1]^{15}$, existe uma configuração de candidatos que produz exatamente $\mathbf{s}$ como saída. Em consequência, o espaço de estados $[0,1]^{15}$ é o mesmo para qualquer valor de $N$ — não há extrapolação fora do domínio de treinamento quando $N$ aumenta.

Formalmente: se a política $\pi_\theta$ foi treinada sobre a distribuição $P_{N=3}(\mathbf{S}_t)$ dos estados produzidos com três candidatos, e se $P_{N=k}(\mathbf{S}_t) \subset [0,1]^{15}$ para todo $k \geq 1$, então $\pi_\theta$ pode generalizar para $k > 3$ sem modificação — o grau de generalização depende da sobreposição entre $P_{N=3}$ e $P_{N=k}$.

Com $N$ maior, a distribuição de $C^*_{\text{best}}$ desloca-se para regiões superiores de $[0,1]$. Formalmente, assumindo que os escores individuais $C^*_i$ são variáveis aleatórias i.i.d. com função de distribuição acumulada $F(c)$ no suporte $[0,1]$, a distribuição da estatística de ordem máxima $C^*_{\text{best}} = \max_{i=1}^N C^*_i$ é:
\begin{equation}
    P\!\left(C^*_{\text{best}} \leq c\right) = \bigl[F(c)\bigr]^N.
    \label{eq:order-stat}
\end{equation}
Para qualquer $c < 1$, tem-se $[F(c)]^N \to 0$ exponencialmente quando $N \to \infty$: a massa de probabilidade colapsa na extremidade superior do intervalo. Em termos do espaço de estados $[0,1]^{15}$, isso significa que o vetor $\mathbf{s}_{\text{best}}$ se desloca continuamente em direção à região $C^*_{\text{best}} \to 1$ — região que é analiticamente possível para qualquer $N$ e, portanto, sobre a qual a rede neural já aprendeu uma representação durante o treinamento com $N = 3$. Como o domínio $[0,1]^{15}$ é estritamente preservado para qualquer $N$, a rede \textbf{interpola} sobre sub-regiões já cobertas por sua topologia interna — não extrapola para fora do domínio de treinamento. Este argumento fundamenta analiticamente a generalização zero-shot sem retreinamento.

A estrutura triplet garante que o agente recebe sempre a mesma forma de informação independentemente de $N$: a melhor opção disponível, o custo local e a dispersão do campo. Com mais candidatos, a melhor opção torna-se objetivamente melhor (maior $C^*_{\text{best}}$), e a política aprende a explorar corretamente esta melhoria porque o sinal de qualidade está contido na mesma dimensão do espaço de estados.

\paragraph{Comparação com abordagens baseadas em grafos.} Métodos baseados em \glspl{GNN} requerem retreinamento completo para cada novo valor de $N$, pois a estrutura do grafo de mensagens possui dimensionalidade $O(N^2 \cdot D_{\text{GNN}})$ por passo de passagem de mensagem — crescendo quadraticamente com o número de nós. A abordagem \gls{TOPSIS} + estado fixo elimina esta dependência estrutural: o custo de inferência cresce apenas como $O(N \log N)$, e nenhum retreinamento é necessário para novos valores de $N$.

% ------------------------------------------------------------
\subsection{Generalização por Regime Operacional}
\label{subsec:generaliz-regime}
% ------------------------------------------------------------

A subseção anterior demonstrou a generalização zero-shot em relação ao número de candidatos $N$. Uma questão independente — e igualmente relevante para o deploy em campo — é: a política $\pi_\theta$ generaliza para regimes operacionais \emph{diferentes} dos vistos no treinamento, tais como novas configurações de hardware, diferentes perfis de carga ou topologias de rede distintas?

A resposta exige distinguir dois mecanismos presentes na representação de estado.

\paragraph{Mecanismo 1 — $C^*$ como estatística de ordem relativa (invariante de escala).}
O coeficiente de proximidade $C^*_i \in [0,1]$ produzido pelo \gls{TOPSIS} compara os candidatos \emph{entre si}, não contra um valor absoluto externo. Formalmente, $C^*_i$ é uma função da geometria interna da matriz $\mathbf{X}$:

\begin{equation}
    C^*_i = \frac{d_i^-}{d_i^+ + d_i^-},
    \label{eq:cstar-regime}
\end{equation}

\noindent onde $d_i^+$ e $d_i^-$ são as distâncias do candidato $i$ à solução ideal positiva e negativa \emph{do conjunto atual}. Consequência direta: se todos os servidores do novo cenário forem uniformemente mais rápidos (e.g., 8 GHz ao invés de 1–4 GHz), a escala absoluta das latências decresce, mas a estrutura de $C^*$ é preservada — o melhor candidato ainda recebe $C^*_\text{best} \approx 1$ e o pior recebe $C^*_\text{worst} \approx 0$. A política aprendeu a interpretar \emph{o nível} de $C^*_\text{best}$ como sinal de qualidade da melhor opção disponível; esse sinal permanece semanticamente correto em qualquer cenário.

\paragraph{Mecanismo 2 — Features absolutas e a envoltória de normalização.}
Os demais quatro atributos do vetor de estado são normalizados por constantes físicas fixas:

\begin{equation}
    \hat\tau_i = \frac{\tau_i}{\tau_{\max}}, \quad
    \hat{C}_i  = \frac{C_i}{C_{\max}},    \quad
    \hat{E}_i  = \frac{E_i}{E_{\max}},
    \label{eq:norm-absoluta}
\end{equation}

\noindent com $\tau_{\max} = 2{,}0$ s, $C_{\max} = 400$ Mbps e $E_{\max} = 0{,}5$ J, escolhidos para cobrir a faixa operacional de redes \gls{VEC}/V2X conforme os modelos de canal \gls{3GPP}. Estas constantes definem uma \textbf{envoltória de normalização}: um novo regime operacional $\mathcal{R}'$ produz estados em $[0,1]^{15}$ — e, portanto, dentro do domínio de treinamento — se e somente se os valores físicos de $\mathcal{R}'$ satisfazem $\tau_i \leq \tau_{\max}$, $C_i \leq C_{\max}$ e $E_i \leq E_{\max}$.

\paragraph{Condição formal de generalização.}
Seja $P_{\mathcal{R}'}(\mathbf{S}_t)$ a distribuição de estados induzida pelo regime $\mathcal{R}'$. A política $\pi_\theta$ generaliza para $\mathcal{R}'$ sem retreinamento se:

\begin{equation}
    P_{\mathcal{R}'}(\mathbf{S}_t) \;\subset\; [0,1]^{15},
    \label{eq:cond-generaliz}
\end{equation}

\noindent condição que é suficiente pelo mesmo argumento de interpolação da Seção~\ref{subsec:zeroshot}: o domínio $[0,1]^{15}$ é denso por construção, e a rede neural não extrapola fora dele. A Tabela~\ref{tab:generaliz-regime} classifica cenários típicos segundo este critério.

\begin{table}[htbp]
\centering
\caption{Generalização da política $\pi_\theta$ para diferentes regimes operacionais.}
\label{tab:generaliz-regime}
\small
\begin{tabular}{p{4.2cm} p{3.5cm} p{2.2cm} p{3.5cm}}
\toprule
\textbf{Mudança de regime} & \textbf{Efeito nas features} & \textbf{Generaliza?} & \textbf{Justificativa} \\
\midrule
Maior $N$ (mais candidatos)
  & $C^*_\text{best}\!\uparrow$, features em $(0,1)$
  & Sim (zero-shot)
  & Sec.~\ref{subsec:zeroshot} \\
\addlinespace
Servidores mais rápidos (ex.: 8 GHz)
  & $\hat\tau\!\downarrow$, $C^*\!\uparrow$; valores dentro de $[0,1]$
  & Sim
  & Interpolação; regime light já cobre $\hat\tau \to 0$ \\
\addlinespace
Carga pesada dentro do envelope ($\tau < 2$ s)
  & $\hat\tau\!\uparrow$; ainda em $(0,1)$
  & Sim
  & Regime heavy cobre $\hat\tau \to 1$ \\
\addlinespace
Carga extrema fora do envelope ($\tau > 2$ s)
  & $\hat\tau = 1$ (saturação por clip)
  & Parcial
  & Perde discriminação absoluta; $C^*$ ainda ranqueia corretamente \\
\addlinespace
Canais de alta capacidade ($C > 400$ Mbps, ex.: mmWave)
  & $\hat{C} = 1$ (saturação por clip)
  & Parcial
  & $C^*$ reflete ganho relativo; sinal absoluto saturado \\
\addlinespace
Novos tipos de nó (topologia diferente)
  & Idem ao caso de maior $N$
  & Sim
  & $N = N_s + N_v$ é indiferente para o estado 15D \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Regimes de carga já cobertos pelo treinamento.}
Os três regimes da cadeia de Markov — \textit{light}, \textit{critical} e \textit{heavy} — são \textbf{latentes}: o agente nunca observa o regime diretamente. Em vez disso, a cadeia de Markov força a política a aprender comportamentos robustos que funcionam bem em todos os regimes simultaneamente, pois cada episódio de treinamento percorre múltiplos regimes. A política não precisa identificar o regime corrente; o estado $\mathbf{S}_t$ já codifica o \emph{efeito} do regime sobre os candidatos disponíveis — filas altas, latências elevadas e baixos $C^*$ no regime heavy; o oposto no regime light.

A Figura~\ref{fig:invariancia-escala} ilustra este princípio com três candidatos avaliados em quatro cenários de escala absolutamente distintos: o \emph{ranking} de $C^*$ é idêntico em todos os casos, mesmo quando a latência absoluta varia de 5 ms (hardware futuro) a 2,8 s (carga extrema que satura $\hat\tau = 1$). A Figura~\ref{fig:envoltoria} complementa esta análise mostrando a distribuição dos pares $(\hat\tau, C^*_\text{best})$ para amostras dos três regimes de treinamento e de dois cenários inéditos: o hardware futuro (servidores de $\sim$10× maior velocidade) projeta-se na região inferior de $[0,1]^2$, dentro do domínio coberto pelo regime \textit{light}; a carga extrema ($\tau > \tau_{\max}$) satura em $\hat\tau = 1$, mas $C^*$ continua a ranquear corretamente os candidatos.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=\linewidth]{fig_invariancia_escala.pdf}
    \caption{Invariância de escala do $C^*$: três candidatos (A, B, C) avaliados em quatro regimes com latências absolutas de 5 ms a 2,8 s. O ranking $A > B > C$ é preservado em todos os cenários, evidenciando que $C^*$ é uma estatística de ordem relativa, independente da escala absoluta dos recursos.}
    \label{fig:invariancia-escala}
\end{figure}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.88\linewidth]{fig_envoltoria_normalizacao.pdf}
    \caption{Envoltória de normalização: distribuição de $(\hat\tau, C^*_\text{best})$ para 600 amostras de cada regime operacional. Os três regimes de treinamento cobrem densamente $[0,1]^2$. O cenário de \emph{hardware futuro} (servidores $\sim$10$\times$ mais rápidos) interpola na região inferior-esquerda — já coberta pelo regime \textit{light}. O cenário de \emph{carga extrema} ($\tau > \tau_{\max}$) satura em $\hat\tau = 1$, mantendo $C^*$ funcional como critério de ranqueamento.}
    \label{fig:envoltoria}
\end{figure}

\paragraph{Implicação prática.}
A arquitetura proposta generaliza por regime operacional dentro da envoltória de normalização definida pelos modelos \gls{3GPP} para redes \gls{VEC}, sem nenhuma modificação estrutural. Para cenários fora dessa envoltória (e.g., redes industriais com latências na ordem de microssegundos, ou canais mmWave de múltiplos gigabits), bastaria reajustar as constantes $\tau_{\max}$, $C_{\max}$ e $E_{\max}$ — sem necessidade de redesenhar a arquitetura ou retreinar com um novo algoritmo.

% ============================================================
\section{Função de Recompensa}
\label{sec:recompensa}
% ============================================================

A função de recompensa deve capturar dois objetivos distintos: (i) minimizar o custo conjunto de latência e energia no regime contínuo, e (ii) garantir o cumprimento de prazos como uma restrição quase-rígida. A recompensa no passo $t$ é definida como:

\begin{equation}
    r_t = -J_t + \Omega_t,
    \label{eq:recompensa}
\end{equation}

\noindent onde o \textbf{custo de desempenho conjunto} é:

\begin{equation}
    J_t = w_1 \cdot \ell_{t,\text{norm}} + w_2 \cdot e_{t,\text{norm}},
    \label{eq:custo-conjunto}
\end{equation}

\noindent com $w_1 = 0{,}7$ e $w_2 = 0{,}3$, e:

\begin{align}
    \ell_{t,\text{norm}} &= \min\!\left(\frac{\tau_t}{\tau_{\max}},\;1{,}0\right), \label{eq:lat-norm} \\
    e_{t,\text{norm}}    &= \min\!\left(\frac{E_t}{E_{\max}},\;1{,}0\right). \label{eq:energy-norm}
\end{align}

A \textbf{penalidade de prazo} é:

\begin{equation}
    \Omega_t = -10 \cdot \mathbf{1}\!\left[\tau_t > D_t\right],
    \label{eq:penalidade-deadline}
\end{equation}

\noindent onde $\mathbf{1}[\cdot]$ é a função indicadora e $D_t$ é o prazo específico da tarefa (dependente do regime corrente, conforme Tabela~\ref{tab:regimes-trafego}).

\paragraph{Justificativa da ponderação $w_1 = 0{,}7$, $w_2 = 0{,}3$.} A ponderação 70/30 é motivada pela literatura \gls{URLLC}: em sistemas de comunicação ultrafidedigna, a latência é o critério primário de qualidade de serviço \cite{uddin2025}. A energia afeta a vida da bateria do veículo, mas tem custo de penalidade menor no curto prazo — ela é um objetivo secundário que guia a otimização fina dentro do conjunto de ações que respeitam os prazos. A normalização pelos respectivos máximos ($\tau_{\max} = 5$ s, $E_{\max} = 1$ J) garante $J_t \in [0,1]$ em toda trajetória possível, assegurando que a hierarquia de sinal entre $J_t$ e $\Omega_t$ seja preservada.

\paragraph{Justificativa da magnitude $|\Omega| = 10$.} A hierarquia de sinal é estabelecida por princípio de projeto: a penalidade deve dominar o custo $J \in [0,1]$ por uma ordem de magnitude, para que a rede neural trate o prazo como uma restrição quase-rígida, e não como mais um critério de otimização. A propriedade é verificável formalmente: para qualquer $J_t \in [0,1]$, tem-se

\begin{equation}
    -J_t + \Omega_t = -J_t - 10 \leq -10 < -1 \leq -J_t \quad (\text{quando } \tau_t \leq D_t).
\end{equation}

Portanto, qualquer trajetória que viole o prazo recebe recompensa estritamente menor que qualquer trajetória que o respeite, independentemente dos valores de latência e energia envolvidos. A hierarquia é preservada para toda trajetória possível \cite{baker2024, farimani2024}.

\paragraph{Justificativa de $\gamma = 0{,}99$.} O fator de desconto deve refletir o horizonte temporal relevante para as dependências entre decisões. No modelo de filas proposto, as filas persistem entre passos com coeficiente $\rho = 0{,}95$ no processo AR(1): um offload ao servidor $i$ no instante $t$ incrementa $Q_i$, impactando $\tau_{queue}$ nos próximos 10--20 passos, até que a drenagem reduza a fila ao nível de equilíbrio. Com $\gamma = 0{,}99$, a recompensa no passo $t + k$ recebe peso $\gamma^k = 0{,}99^k$; para $k = 100$ passos futuros, este peso é $0{,}99^{100} \approx 0{,}366$ — suficiente para capturar o horizonte de drenagem de fila e motivar o agente a evitar congestionamento de longo prazo. Um $\gamma$ menor (e.g., 0,95) atribuiria peso $0{,}95^{100} \approx 0{,}006$ ao passo $t+100$, tornando o agente míope frente ao efeito de acumulação de filas.

O \textbf{retorno acumulado esperado} com desconto $\gamma = 0{,}99$ é:

\begin{equation}
    G_t = \sum_{k=0}^{\infty} \gamma^k \cdot r_{t+k}.
    \label{eq:retorno-acumulado}
\end{equation}

% ============================================================
\section{Algoritmo de Treinamento: PPO}
\label{sec:ppo}
% ============================================================

O agente é treinado com o algoritmo \textbf{\gls{PPO}} (\textit{Proximal Policy Optimization}), um algoritmo Ator-Crítico \textit{on-policy} da família de métodos de gradiente de política. Esta seção apresenta a formulação matemática completa do \gls{PPO} e justifica sua escolha frente às alternativas.

% ------------------------------------------------------------
\subsection{Estrutura Ator-Crítico}
\label{subsec:ator-critico}
% ------------------------------------------------------------

O \gls{PPO} mantém duas redes neurais com parâmetros independentes:

\begin{itemize}
    \item \textbf{Ator (rede de política):} $\pi_\theta: \mathcal{S} \to \Delta(\mathcal{A})$, mapeia o estado $\mathbf{S}_t \in \mathbb{R}^{15}$ para uma distribuição de probabilidade sobre as ações $\mathcal{A} = \{0, 1\}$. O ator é implementado como um Perceptron Multicamadas (MLP) com arquitetura $[15 \to 256 \to 256 \to 2]$ (duas camadas ocultas de 256 neurônios com ativação ReLU, camada de saída com \textit{softmax}).

    \item \textbf{Crítico (rede de valor):} $V_\phi: \mathcal{S} \to \mathbb{R}$, estima o retorno esperado $V^\pi(\mathbf{S}_t) = \mathbb{E}_\pi[G_t \mid \mathbf{S}_t]$. O crítico possui arquitetura idêntica ao ator, exceto na camada de saída: $[15 \to 256 \to 256 \to 1]$.
\end{itemize}

A escolha da arquitetura MLP $[256,256]$ é fundamentada pelo \textbf{Teorema da Aproximação Universal} \cite{hornik1989}: toda função mensurável de Borel $g: [0,1]^{15} \to \mathbb{R}$ pode ser aproximada com precisão arbitrária por uma rede neural de uma única camada oculta de largura suficiente e ativações não-polinomiais. Uma MLP com \textit{duas} camadas ocultas aumenta a profundidade e reduz a largura necessária para representar funções com descontinuidades — como a fronteira induzida pela penalidade de prazo $\Omega_t = -10 \cdot \mathbf{1}[\tau_t > D_t]$, que introduz uma superfície de decisão abrupta no espaço compacto $[0,1]^{15}$.

A largura de 256 neurônios é motivada pela restrição operacional de inferência em tempo real: a complexidade de inferência do MLP é $O(H \cdot D)$, onde $H = 256$ e $D = 15$, garantindo tempo de resposta $O(1)$ compatível com os requisitos de latência \gls{URLLC}. O número total de parâmetros, $\approx 270{.}000$, limita a dimensão do espaço de parâmetros e satisfaz a condição de complexidade de Vapnik-Chervonenkis (VC) suficiente para a aprendizagem de fronteiras de decisão no domínio $[0,1]^{15}$ com garantias de generalização.

Ambas as redes são implementadas com a biblioteca \textit{stable-baselines3}, utilizando a política \texttt{MlpPolicy} com \texttt{net\_arch=[256, 256]} para ator e crítico.

% ------------------------------------------------------------
\subsection{Estimativa de Vantagem com GAE}
\label{subsec:gae}
% ------------------------------------------------------------

A \textbf{função de vantagem} $A_t = Q(s_t, a_t) - V(s_t)$ mede o quanto a ação $a_t$ é melhor (ou pior) do que a ação média sob a política $\pi$. Estimativas de vantagem com baixa variância são críticas para o aprendizado estável em ambientes com penalidades esparsas e grandes como a presente proposta.

O \textbf{GAE} (\textit{Generalized Advantage Estimation}) computa uma estimativa suavizada da vantagem:

\begin{equation}
    \hat{A}_t^{\text{GAE}} = \sum_{l=0}^{\infty} (\gamma \lambda)^l \cdot \delta_{t+l},
    \label{eq:gae}
\end{equation}

\noindent onde $\delta_t = r_t + \gamma V_\phi(s_{t+1}) - V_\phi(s_t)$ é o \textbf{erro de diferença temporal} (TD error) e $\lambda = 0{,}95$ controla o \textit{trade-off} viés-variância: $\lambda \to 0$ reduz variância ao custo de maior viés (equivale ao TD(0)); $\lambda \to 1$ elimina o viés ao custo de maior variância (equivale ao retorno de Monte Carlo).

% ------------------------------------------------------------
\subsection{Objetivo Proximal (CLIP)}
\label{subsec:clip}
% ------------------------------------------------------------

O \gls{PPO} otimiza um objetivo substituto (\textit{surrogate objective}) que limita o tamanho da atualização da política. Define-se a razão de probabilidade:

\begin{equation}
    r_t(\theta) = \frac{\pi_\theta(a_t \mid s_t)}{\pi_{\theta_{\text{old}}}(a_t \mid s_t)},
    \label{eq:prob-ratio}
\end{equation}

\noindent e o objetivo CLIP é:

\begin{equation}
    L^{\text{CLIP}}(\theta) = \mathbb{E}_t\!\left[\min\!\left(r_t(\theta)\,\hat{A}_t^{\text{GAE}},\;\mathrm{clip}\!\left(r_t(\theta),\;1-\varepsilon,\;1+\varepsilon\right)\hat{A}_t^{\text{GAE}}\right)\right],
    \label{eq:ppo-clip}
\end{equation}

\noindent com $\varepsilon = 0{,}2$. O parâmetro $\varepsilon$ define a \textbf{região de confiança} dentro da qual a aproximação linear da função de vantagem $\hat{A}_t^{\text{GAE}}$ permanece válida: ao restringir $r_t(\theta) \in [1-\varepsilon,\, 1+\varepsilon]$, o mecanismo CLIP garante que a atualização da política permaneça na vizinhança local onde a estimativa de vantagem é confiável. Analiticamente, \citeonline{schulman2017ppo} demonstra que o objetivo CLIP é um limite inferior (\textit{surrogate lower bound}) do retorno esperado $J(\theta)$: maximizá-lo garante melhoria monotônica de $J(\theta)$ a cada iteração, evitando o colapso da política diante das penalidades esparsas de $\Omega = -10$, que introduzem descontinuidades severas na superfície de recompensa. O valor $\varepsilon = 0{,}2$ é o padrão da literatura \cite{schulman2017ppo}, oferecendo equilíbrio entre estabilidade (restrição da região de confiança) e velocidade de convergência (tamanho do passo de atualização). O objetivo completo do \gls{PPO} incorpora também a perda do crítico e o bônus de entropia:

\begin{equation}
    L(\theta, \phi) = L^{\text{CLIP}}(\theta) - c_1 \cdot L^{\text{VF}}(\phi) + c_2 \cdot \mathcal{H}[\pi_\theta(\cdot \mid s_t)],
    \label{eq:ppo-objetivo-completo}
\end{equation}

\noindent onde $L^{\text{VF}}(\phi) = \mathbb{E}_t[(V_\phi(s_t) - G_t)^2]$ é o erro quadrático médio do crítico, $\mathcal{H}[\pi_\theta(\cdot \mid s_t)] = -\sum_{a} \pi_\theta(a \mid s_t) \log \pi_\theta(a \mid s_t)$ é a entropia da política, e $c_2 = 0{,}01$ é o coeficiente de entropia (\texttt{ent\_coef}).

Cada componente do objetivo cumpre uma função específica:

\begin{itemize}
    \item \textbf{CLIP:} Quando $r_t(\theta) > 1 + \varepsilon$ (a ação tornou-se mais provável sob a nova política), o gradiente é zerado, impedindo atualizações excessivas que poderiam desestabilizar a política — em particular, evita o colapso da política ao explorar a penalidade de $-10$ de forma gulosa e prematura.

    \item \textbf{Perda do Crítico $L^{\text{VF}}$:} Treina o crítico para predizer com precisão os retornos esperados, fornecendo estimativas de vantagem mais precisas para o ator.

    \item \textbf{Bônus de Entropia $\mathcal{H}$:} Penaliza políticas determinísticas, incentivando a exploração nas fases iniciais do treinamento, quando o agente ainda não mapeou bem o espaço de estados.
\end{itemize}

Os hiperparâmetros de treinamento são: $n_{\text{steps}} = 2048$, $\text{batch\_size} = 64$, $\gamma = 0{,}99$, $\lambda_{\text{GAE}} = 0{,}95$, $\varepsilon_{\text{clip}} = 0{,}2$, $\alpha = 3 \times 10^{-4}$ (\textit{learning rate}).

% ------------------------------------------------------------
\subsection{Justificativa da Escolha do PPO}
\label{subsec:ppo-justificativa}
% ------------------------------------------------------------

A escolha do \gls{PPO} frente às principais alternativas — \gls{DQN}, \gls{SAC} e DDPG — é motivada por três fatores estruturais do problema:

\begin{enumerate}
    \item \textbf{Penalidades esparsas e grandes:} A penalidade de $\Omega = -10$ na violação de prazo cria uma superfície de recompensa altamente não-suave. Métodos baseados em valor como o \gls{DQN} podem sofrer de superestimação catastrófica da função de valor próximo a estados de penalidade \cite{miriyala2025}. O mecanismo CLIP do \gls{PPO} limita a magnitude do gradiente, prevenindo divergência.

    \item \textbf{Ambiente não-estacionário:} A cadeia de Markov de regimes causa \textit{distribution shift} contínuo durante o treinamento. O \gls{PPO} é \textit{on-policy}: cada atualização usa exclusivamente dados gerados pela política corrente, evitando os artefatos de dados obsoletos (\textit{stale data}) que afetam métodos \textit{off-policy} como o \gls{SAC} e o DDPG, cujo \textit{experience replay} pode conter transições de regimes já superados.

    \item \textbf{Espaço de ação discreto e binário:} A ação $\{0, 1\}$ é naturalmente representada por uma distribuição categórica com saída \textit{softmax}, sem necessidade de aproximações de ação contínua como as requeridas pelo DDPG. O \gls{PPO} com ações discretas é matematicamente direto e não requer truques adicionais de exploração \cite{uddin2025, wei2024}.
\end{enumerate}

% ============================================================
\section{Complexidade Computacional}
\label{sec:complexidade}
% ============================================================

A viabilidade da abordagem para operação em tempo real em redes \gls{VEC} depende da eficiência computacional por passo de decisão. A Tabela~\ref{tab:complexidade} sumariza a complexidade assintótica de cada componente.

\begin{table}[htbp]
\centering
\caption{Complexidade computacional por passo de decisão ($N$: número de candidatos; $D = 15$: dimensão do estado; $H = 256$: neurônios por camada oculta).}
\label{tab:complexidade}
\begin{tabular}{lll}
\hline
\textbf{Componente} & \textbf{Complexidade} & \textbf{Observação} \\
\hline
Normalização TOPSIS        & $O(N \cdot n)$   & $n = 4$ fixo \\
Distâncias TOPSIS          & $O(N \cdot n)$   & \\
Ordenação TOPSIS           & $O(N \log N)$    & dominante \\
Inferência PPO (MLP)       & $O(H \cdot D)$   & $O(1)$ \\
\hline
\textbf{Total por passo}   & $O(N \log N)$    & dominado pelo TOPSIS \\
\hline
\end{tabular}
\end{table}

Para $N \leq 20$ candidatos, típico em cenários \gls{VEC} urbanos, a complexidade é equivalente a ordenar 20 números — operação da ordem de microssegundos em hardware moderno, plenamente compatível com os requisitos de tempo real (decisão $< 1$ ms).

\paragraph{Complexidade espacial.} A rede \gls{PPO} com arquitetura $[15 \to 256 \to 256 \to 2]$ para o ator e $[15 \to 256 \to 256 \to 1]$ para o crítico totaliza $2 \times (15 \times 256 + 256^2 + 256 \times 2) \approx 270{.}000$ parâmetros $\approx 1{,}0$ MB. O sistema completo cabe confortavelmente na memória de qualquer \gls{RSU} ou \gls{gNB} contemporâneo.

\paragraph{Comparação com alternativas.} Uma abordagem baseada em \gls{GNN} para lidar com a topologia variável exigiria, em contraste, computação de mensagens entre $O(N^2)$ pares de nós por camada, resultando em complexidade $O(N^2 \cdot D_{\text{GNN}})$ por passo — quadrática em $N$ e potencialmente proibitiva para $N$ grande ou em hardware embarcado \cite{miriyala2025}. A abordagem \gls{TOPSIS} + estado fixo reduz esta dependência para $O(N \log N)$, sem perda de expressividade na representação do estado, e elimina a necessidade de retreinamento para novos valores de $N$ (Seção~\ref{subsec:zeroshot}).
